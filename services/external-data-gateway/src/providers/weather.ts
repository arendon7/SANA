import type { ProviderMetricSample, ProviderNormalizer, RawExternalEnvelope } from '@agroway/external-data-contracts';type WeatherPayload={timestamp?:unknown;temperatureC?:unknown;relativeHumidityPct?:unknown;rainfallMm?:unknown;windSpeedMs?:unknown;solarRadiationWm2?:unknown;};export class CanonicalWeatherJsonNormalizer implements ProviderNormalizer{readonly providerKey='weather-json';samples(envelope:RawExternalEnvelope):readonly ProviderMetricSample[]{const p=(envelope.payload??{}) as WeatherPayload;const observedAt=String(p.timestamp??envelope.observedAt??'');if(!observedAt)throw new Error('WEATHER_TIMESTAMP_MISSING');const candidates=[['AIR_TEMPERATURE',p.temperatureC,'Cel'],['RELATIVE_HUMIDITY',p.relativeHumidityPct,'%'],['RAINFALL',p.rainfallMm,'mm'],['WIND_SPEED',p.windSpeedMs,'m/s'],['SOLAR_RADIATION',p.solarRadiationWm2,'W/m2']] as const;return candidates.filter(([,v])=>v!==undefined&&v!==null).map(([metric,v,unit])=>({observedAt,metric,value:Number(v),unit}));}}
