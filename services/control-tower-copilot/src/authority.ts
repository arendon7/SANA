import type { AuthorityDecision, CopilotInquiry } from '@agroway/copilot-knowledge-contracts';
function normalize(text:string):string{return text.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();}
const DIRECT_TRANSACTIONAL=[/^(please\s+)?(approve|authorize|execute|deploy|transfer|commit|cancel|close|activate|pause)\b/,/^(por favor\s+)?(aprueba|aprobar|autoriza|autorizar|ejecuta|ejecutar|desembolsa|desembolsar|transfiere|transferir|compromete|cancelar|activa|activar|pausa|pausar)\b/,/\b(i want you to|i need you to|can you|could you)\s+(approve|authorize|execute|deploy|transfer|commit|cancel|close|activate|pause)\b/,/\b(quiero que|necesito que|puedes|podrias)\s+(apruebes|aprobar|autorices|autorizar|ejecutes|ejecutar|desembolses|desembolsar|transfieras|transferir|canceles|cancelar|actives|activar|pauses|pausar)\b/];
const DIRECT_MUTATION=[/^(please\s+)?(change|update|modify|delete|write)\b.{0,50}\b(project|budget|capital|risk|alert|order)\b/,/^(por favor\s+)?(cambia|actualiza|modifica|elimina|escribe)\b.{0,50}\b(proyecto|presupuesto|capital|riesgo|alerta|orden)\b/];
export function evaluateAuthority(inquiry:CopilotInquiry,at:string):AuthorityDecision {const q=normalize(inquiry.question); const reasons:string[]=[];if(DIRECT_TRANSACTIONAL.some(x=>x.test(q))) reasons.push('TRANSACTIONAL_AUTHORITY_FORBIDDEN');if(DIRECT_MUTATION.some(x=>x.test(q))) reasons.push('DOMAIN_MUTATION_FORBIDDEN');return {allowed:reasons.length===0,reasonCodes:[...new Set(reasons)].sort(),evaluatedAt:at};}
