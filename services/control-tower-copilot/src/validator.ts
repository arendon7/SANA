import type { CopilotMode, EvidenceBundle, ModelStructuredOutput } from '@agroway/copilot-knowledge-contracts';
const AUTHORITY_CLAIM=/\b(i|we)\s+(have\s+)?(approved|authorized|executed|deployed|transferred)\b|\b(he|hemos)\s+(aprobado|autorizado|ejecutado|desembolsado|transferido)\b/i;
export function validateModelOutput(output:ModelStructuredOutput,bundle:EvidenceBundle,mode:CopilotMode):readonly string[]{const failures:string[]=[]; const allowed=new Set(bundle.acceptedEvidenceIds);if(output.citedEvidenceIds.length===0) failures.push('CITATION_REQUIRED');for(const id of output.citedEvidenceIds) if(!allowed.has(id)) failures.push(`UNKNOWN_CITATION:${id}`);if(!output.answer.trim()) failures.push('EMPTY_ANSWER');if(AUTHORITY_CLAIM.test(output.answer)) failures.push('AUTHORITY_CLAIM_FORBIDDEN');if(output.draftSuggestion && mode!=='DRAFT') failures.push('DRAFT_NOT_ALLOWED_IN_MODE');if(output.draftSuggestion && !output.draftSuggestion.body.trim()) failures.push('EMPTY_DRAFT');return [...new Set(failures)].sort();}
