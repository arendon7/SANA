# Graphify — Investment Project Workspace

`InvestmentProject`
→ `ProductionRef(producer/farm/plots/cycles)`
→ `ApprovedBudgetVersion`
→ `CapitalCommitments`
→ `CapitalDeployments`
→ `Risks + Evidence`
→ `Demand + ImpactSnapshot`

`Canonical project evidence`
→ `Control Tower Copilot`
→ `DRAFT_SUGGESTION`
→ `Human review`

Forbidden:

- `UI/Copilot → deploy capital`
- `UI/Copilot → approve budget`
- `UI/Copilot → change project state`
- `local review → canonical mutation`
- `demo → certification`

D10: `PENDING`.
