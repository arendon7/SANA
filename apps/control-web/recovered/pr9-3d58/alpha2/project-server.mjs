import http from 'node:http';import{readFile,stat}from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';const here=path.dirname(fileURLToPath(import.meta.url)),publicDir=path.join(here,'public'),port=Number(process.env.PORT||4274),host=process.env.HOST||'127.0.0.1';const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.webmanifest':'application/manifest+json; charset=utf-8'};const headers={'X-Content-Type-Options':'nosniff','X-Frame-Options':'DENY','Referrer-Policy':'no-referrer','Cache-Control':'no-store'};const safe=p=>{const c=path.normalize(path.join(publicDir,decodeURIComponent(p)));return c.startsWith(publicDir)?c:null};const server=http.createServer(async(req,res)=>{try{let pathname=new URL(req.url||'/',`http://${req.headers.host||'localhost'}`).pathname;if(pathname==='/'||pathname.startsWith('/control/projects/'))pathname='/project.html';else if(pathname==='/control')pathname='/index.html';let file=safe(pathname);if(!file)throw new Error('INVALID_PATH');const s=await stat(file).catch(()=>null);if(!s?.isFile())file=path.join(publicDir,'project.html');const bytes=await readFile(file);res.writeHead(200,{...headers,'Content-Type':types[path.extname(file)]||'application/octet-stream'});res.end(bytes);}catch{res.writeHead(404,{...headers,'Content-Type':'text/plain'});res.end('Not found');}});server.listen(port,host,()=>console.log(`GREENATICS_CONTROL_PROJECT_REVIEW http://${host}:${port}/control/projects/inv-yar-001`));
