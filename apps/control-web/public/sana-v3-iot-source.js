(() => {
  'use strict';

  const LAYERS=[
    {id:'water',name:'Fuentes de agua',variables:[['Temperatura','22.4 °C','OBSERVADO DEMO'],['pH','6.4','OBSERVADO DEMO'],['Electroconductividad','0.82 mS/cm','OBSERVADO DEMO']],status:'3/3 variables modeladas'},
    {id:'environment',name:'Ambiente',variables:[['Temperatura','24.8 °C','OBSERVADO DEMO'],['Humedad relativa','71 %','OBSERVADO DEMO'],['CO₂','428 ppm','OBSERVADO DEMO'],['Radiación solar directa','612 W/m²','ESTIMACIÓN DEMO'],['Radiación solar indirecta','184 W/m²','ESTIMACIÓN DEMO']],status:'5/5 variables modeladas'},
    {id:'soil',name:'Suelo',variables:[['Temperatura','21.6 °C','OBSERVADO DEMO'],['Humedad','43 %','OBSERVADO DEMO'],['Electroconductividad','1.12 mS/cm','OBSERVADO DEMO'],['Fósforo','—','SIN SENSOR REAL'],['Potasio','—','SIN SENSOR REAL'],['Nitrógeno','—','SIN SENSOR REAL']],status:'3 observadas · 3 sin sensor real'}
  ];

  function latestLocal(){return storage.records.filter(r=>r.type==='sensor').slice(-6).reverse()}

  function sourceIot(){
    const local=latestLocal();
    return `${head('AGROWAY · IOT Y CAPTURA OFFLINE','La matriz original de variables, ahora con confianza explícita.','AGROWAY separa agua, ambiente y suelo. SANA conserva la matriz y distingue captura, procedencia, calidad y sincronización: una lectura aislada nunca autoriza manejo agronómico y un estado de nube de cuenta no equivale a ACK del registro.',`<button class="btn primary" data-iot-source-reading>Registrar lectura DEMO</button>`)}
      <section class="grid metrics">${metric('Capas instrumentables','3','agua · ambiente · suelo','good')}${metric('Variables fuente','14','definidas en AGROWAY histórico','good')}${metric('Lecturas usuario',local.length,'capturas locales; ACK evaluado por registro',local.length?'good':'warn')}${metric('Decisiones automáticas','0','HUMAN_REVIEW_REQUIRED','warn')}</section>
      <section class="grid three">${LAYERS.map(layer=>`<article class="card"><div class="card-head"><div><h2>${esc(layer.name)}</h2><p>${esc(layer.status)}</p></div><span class="status">SOURCE MATRIX</span></div><div class="card-body">${layer.variables.map(v=>`<div class="mini-row"><span>${esc(v[0])}</span><b>${esc(v[1])}</b><small>${esc(v[2])}</small></div>`).join('')}</div></article>`).join('')}</section>
      <section class="grid split" style="margin-top:14px"><article class="card"><div class="card-head"><div><h2>Humedad suelo · AGU-A2</h2><p>Serie sintética usada para demostrar tendencia y no para ejecutar riego.</p></div><span class="status warn">DEMO</span></div><div class="card-body">${chart()}<div class="section-note" style="margin-top:12px">La lectura puede alimentar observación y alerta. SOURCE_LABEL ≠ SOURCE_AUTHENTICITY · READING ≠ VALIDATED_MEASUREMENT · VALIDATED_MEASUREMENT ≠ MANAGEMENT_DECISION.</div></div></article><article class="card"><div class="card-head"><div><h2>Lecturas registradas por esta cuenta</h2><p>Persistencia local visible sin promover el estado de cuenta a ACK individual.</p></div></div><div class="card-body">${local.length?local.map(r=>`<div class="row"><span class="dot"></span><div class="copy"><strong>${esc(r.values?.variable||r.title||'Lectura')}</strong><span>${esc(r.lot)} · ${esc(r.values?.value||'—')} ${esc(r.values?.unit||'')} · ${esc(r.values?.source||'fuente no capturada')}</span></div><div class="meta">${new Date(r.createdAt).toLocaleDateString('es-CO')}<br><span class="status warn">LOCAL_ONLY_NO_RECORD_ACK</span></div></div>`).join(''):'<div class="empty">Aún no has registrado lecturas con esta identidad.</div>'}<div class="section-note" style="margin-top:12px">Una captura local puede formar parte del estado DEMO sincronizable de la cuenta, pero ACCOUNT_SYNC_STATUS ≠ RECORD_ACK. No se certifica hardware, calibración, laboratorio ni proveedor externo.</div></div></article></section>
      <section class="card" style="margin-top:14px"><div class="card-head"><div><h2>Contrato de captura</h2><p>Qué debe conservar cada lectura antes de cualquier interpretación.</p></div></div><div class="card-body"><div class="flow"><div class="flow-step"><b>01</b><span>Fuente</span><small>manual / sensor / import</small></div><div class="flow-step"><b>02</b><span>Variable</span><small>valor + unidad</small></div><div class="flow-step"><b>03</b><span>Ubicación</span><small>lote + punto</small></div><div class="flow-step"><b>04</b><span>Momento</span><small>observado + capturado</small></div><div class="flow-step"><b>05</b><span>Método</span><small>dispositivo / calibración</small></div><div class="flow-step"><b>06</b><span>Confianza</span><small>calidad + ACK + conflicto</small></div></div><div class="section-note" style="margin-top:12px">CAPTURED ≠ SYNCED · SYNC_ATTEMPT ≠ SERVER_ACK · SERVER_ACK ≠ HARDWARE_VERIFIED · NETWORK_ONLINE ≠ RECORD_SYNCED.</div></div></section>${footer()}`;
  }

  views.iot=sourceIot;
  window.__SANA_IOT_SOURCE__=Object.freeze({layers:()=>LAYERS.map(layer=>({...layer,variables:layer.variables.map(v=>[...v])})),localReadings:()=>latestLocal().map(r=>({...r,values:{...(r.values||{})}})),integrity:'READING ≠ VALIDATED_MEASUREMENT · ACCOUNT_SYNC_STATUS ≠ RECORD_ACK · HUMAN_REVIEW_REQUIRED'});

  function openReading(){
    const lotOptions=DEMO.lots.map(l=>`<option value="${l.id}">${l.id} · ${esc(l.crop)}</option>`).join('');
    const variableOptions=LAYERS.flatMap(layer=>layer.variables.map(v=>`${layer.name} · ${v[0]}`)).map(v=>`<option>${esc(v)}</option>`).join('');
    const body=`<div class="fields"><label>Lote<select name="lot">${lotOptions}</select></label><label>Variable<select name="variable">${variableOptions}</select></label><label>Valor<input name="value" type="number" step="0.01" required></label><label>Unidad<input name="unit" required placeholder="%, °C, pH, mS/cm, ppm..."></label><label>Momento observado<input name="observedAt" type="datetime-local"></label><label>Punto / muestra<input name="point" placeholder="Ej. P3 / S2"></label><label>Fuente<select name="source"><option>Lectura manual DEMO</option><option>Sensor DEMO</option><option>Dato importado DEMO</option></select></label><label>Dispositivo / fuente ref.<input name="deviceRef" placeholder="Opcional · identificador declarado"></label><label>Método<input name="method" placeholder="Ej. sonda manual / sensor capacitivo"></label><label>Calibración<select name="calibration"><option>NOT_CAPTURED</option><option>CALIBRATION_NOT_CAPTURED</option><option>DECLARED_DEMO_NOT_VERIFIED</option></select></label><label>Calidad declarada<select name="quality"><option>OBSERVADO DEMO</option><option>ESTIMACIÓN DEMO</option><option>PENDIENTE VERIFICACIÓN</option></select></label><label>Persistencia<input value="LOCAL CAPTURE · RECORD ACK NOT INFERRED" readonly></label><label class="full">Contexto / observación<textarea name="detail" placeholder="Condición, ubicación, motivo y cualquier limitación de la captura..."></textarea></label><label class="full">Autoridad<input value="HUMAN_REVIEW_REQUIRED · NO EJECUTA MANEJO" readonly></label></div>`;
    openModal('IOT / CAPTURA · AGROWAY','Registrar lectura DEMO',body,true,'sensor');
  }

  document.addEventListener('click',event=>{if(event.target.closest('[data-iot-source-reading]'))openReading()});
})();
