(() => {
  'use strict';
  const base=window.__SANA_COMMERCIAL_LEDGER__;
  if(!base?.cases||base.schema!=='SANA_COMMERCIAL_OFFTAKE_LEDGER_V1')return;
  const VERSION='V147';
  const INTEGRITY='COMMERCIAL_REFERENCE ≠ COMMERCIAL_EXECUTION · BUYER_REF ≠ BUYER_IDENTITY_VERIFIED · SUPPORT_REFERENCE ≠ EVIDENCE_VERIFIED · DELIVERY_REFERENCE ≠ BUYER_ACCEPTANCE · HARVEST_REFERENCE ≠ COMMERCIAL_SUCCESS · ECONOMIC_REFERENCE ≠ ACCOUNTING_VERIFICATION · AGREEMENT_REFERENCE_DECLARED ≠ VERIFIED_CONTRACT · PRICE_REFERENCE_DECLARED ≠ REALIZED_PRICE · INVOICE_REFERENCE_DECLARED ≠ VERIFIED_INVOICE · PAYMENT_STATUS_DECLARED ≠ PAYMENT_EXECUTED · RECEIPT_REFERENCE_DECLARED ≠ BANK_SETTLEMENT · REFERENCE ≠ GUARANTEED_REVENUE ≠ CREDIT_RISK ≠ ELIGIBILITY ≠ INVESTMENT_SIGNAL';
  const COMMERCIAL_TARGET_KINDS=new Set(['OFFER_REGISTERED','BUYER_INTEREST','NEGOTIATION_NOTE','OFFTAKE_AGREEMENT_REFERENCE','DELIVERY_COMMITMENT','DELIVERY_DECLARATION','BUYER_ACCEPTANCE_DECLARATION','INVOICE_REFERENCE','PAYMENT_STATUS_DECLARED','CASH_RECEIPT_DECLARATION']);
  function metadata(){const out=new Map();(storage?.records||[]).filter(r=>r.type==='commercial-reference-meta'&&r.values?.sourceSchema===base.schema).forEach(r=>{const id=r.values?.caseId||'';if(id)out.set(id,{referenceVersion:r.values?.referenceVersion||'',recordId:r.id||''})});return out}
  function sameScope(a,b){return !a||!b||a===b}
  function temporal(source,target){const a=Date.parse(source?.observedAt||''),b=Date.parse(target?.observedAt||'');return Number.isFinite(a)&&Number.isFinite(b)&&b>a?'FORWARD_REFERENCE':null}
  function harvestEvent(ref){for(const c of window.__SANA_HARVEST_LEDGER__?.cases?.()||[]){const e=(c.events||[]).find(x=>x.id===ref);if(e)return {case:c,event:e}}return null}
  function economicEvent(ref){for(const c of window.__SANA_ECONOMIC_RECONCILIATION__?.cases?.()||[]){const e=(c.events||[]).find(x=>x.id===ref);if(e)return {case:c,event:e}}return null}
  function supportRef(c,e,ref,all){if(!ref)return {status:'MISSING_REFERENCE',domain:'COMMERCIAL_EVENT',target:null};let found=null,owner=null;for(const x of all){const t=(x.events||[]).find(z=>z.id===ref);if(t){found=t;owner=x;break}}if(!found)return {status:'MISSING_TARGET',domain:'COMMERCIAL_EVENT',target:null};if(owner?.id!==c.id)return {status:'CROSS_CASE_REFERENCE',domain:'COMMERCIAL_EVENT',target:found};if(!COMMERCIAL_TARGET_KINDS.has(found.kind))return {status:'KIND_MISMATCH',domain:'COMMERCIAL_EVENT',target:found};const time=temporal(e,found);if(time)return {status:time,domain:'COMMERCIAL_EVENT',target:found};return {status:'LINKED',domain:'COMMERCIAL_EVENT',target:found}}
  function deliveryRef(c,e){if(!e.deliveryRef)return {status:'MISSING_REFERENCE',domain:'HARVEST',target:null};const t=harvestEvent(e.deliveryRef);if(!t)return {status:'MISSING_TARGET',domain:'HARVEST',target:null};if(!sameScope(c.lot,t.case?.lot||t.event?.lot))return {status:'CROSS_SCOPE_REFERENCE',domain:'HARVEST',target:t.event};if(t.event?.kind!=='HANDOFF')return {status:'KIND_MISMATCH',domain:'HARVEST',target:t.event};const time=temporal(e,t.event);if(time)return {status:time,domain:'HARVEST',target:t.event};return {status:'LINKED',domain:'HARVEST',target:t.event}}
  function crossRef(c,x){if(!x.sourceRef)return {status:'MISSING_REFERENCE',domain:x.sourceDomain||'',target:null};if(x.sourceDomain==='HARVEST'){const t=harvestEvent(x.sourceRef);if(!t)return {status:'MISSING_TARGET',domain:'HARVEST',target:null};if(!sameScope(c.lot,t.case?.lot||t.event?.lot))return {status:'CROSS_SCOPE_REFERENCE',domain:'HARVEST',target:t.event};const expected=x.kind==='HARVEST_HANDOFF_REFERENCE'?'HANDOFF':x.kind==='HARVEST_SALE_DECLARATION_REFERENCE'?'SALE_DECLARATION':null;if(!expected)return {status:'UNSUPPORTED_REFERENCE_KIND',domain:'HARVEST',target:t.event};if(t.event?.kind!==expected)return {status:'KIND_MISMATCH',domain:'HARVEST',target:t.event};return {status:'LINKED',domain:'HARVEST',target:t.event}}
    if(x.sourceDomain==='ECONOMICS'){const t=economicEvent(x.sourceRef);if(!t)return {status:'MISSING_TARGET',domain:'ECONOMICS',target:null};if(!sameScope(c.lot,t.case?.lot||t.event?.lot))return {status:'CROSS_SCOPE_REFERENCE',domain:'ECONOMICS',target:t.event};if(x.sourceKind&&t.event?.kind!==x.sourceKind)return {status:'KIND_MISMATCH',domain:'ECONOMICS',target:t.event};return {status:'LINKED',domain:'ECONOMICS',target:t.event}}
    return {status:'UNSUPPORTED_SOURCE_DOMAIN',domain:x.sourceDomain||'',target:null}}
  function canonicalRows(c,all){if(metadata().get(c.id)?.referenceVersion!==VERSION)return [];const rows=[];for(const e of c.events||[]){if(e.kind==='EVIDENCE'){const refs=(e.supports||[]).length?e.supports:[''];refs.forEach(ref=>rows.push({sourceEventId:e.id,sourceKind:e.kind,kind:'COMMERCIAL_SUPPORT_REF',refId:ref||'',reference:supportRef(c,e,ref,all)}))}if(e.kind==='DELIVERY_DECLARATION')rows.push({sourceEventId:e.id,sourceKind:e.kind,kind:'HARVEST_DELIVERY_REF',refId:e.deliveryRef||'',reference:deliveryRef(c,e)})}for(const x of c.crossDomainRefs||[])rows.push({sourceEventId:x.id,sourceKind:x.kind,kind:'CROSS_DOMAIN_REF',refId:x.sourceRef||'',sourceDomain:x.sourceDomain||'',sourceKindExpected:x.sourceKind||'',reference:crossRef(c,x)});return rows}
  function declaredRows(c){const out=[];for(const e of c.events||[]){for(const [field,kind] of [['buyerRef','BUYER_REF_DECLARED'],['agreementRef','AGREEMENT_REF_DECLARED'],['priceRef','PRICE_REF_DECLARED'],['invoiceRef','INVOICE_REF_DECLARED'],['paymentState','PAYMENT_STATE_DECLARED'],['receiptRef','RECEIPT_REF_DECLARED'],['evidenceRef','EVIDENCE_REF_DECLARED']])if(e[field]!==undefined&&e[field]!==null&&e[field]!=='')out.push({sourceEventId:e.id,kind,refId:String(e[field]),status:'DECLARED_NON_CANONICAL_REFERENCE'})}for(const x of c.crossDomainRefs||[])if(x.commercialRef)out.push({sourceEventId:x.id,kind:'COMMERCIAL_REF_DECLARED',refId:String(x.commercialRef),status:'DECLARED_NON_CANONICAL_REFERENCE'});return out}
  function caseFor(id){const all=base.cases(),c=all.find(x=>x.id===id);if(!c)return null;const meta=metadata().get(id)||{},captured=meta.referenceVersion===VERSION,rows=canonicalRows(c,all),linked=rows.filter(r=>r.reference.status==='LINKED').length;return {...c,referenceVersion:meta.referenceVersion||'',referenceState:captured?'CAPTURED_V147':'LEGACY_REFERENCE_NOT_CAPTURED',referenceCoverage:{linked,total:rows.length,percent:rows.length?Math.round(linked/rows.length*100):null},referenceIssues:rows.length-linked,referenceRows:rows,declaredReferenceRows:declaredRows(c),integrity:`${c.integrity} · ${INTEGRITY}`}}
  function cases(){return base.cases().map(c=>caseFor(c.id)).filter(Boolean)}
  function forLot(lot){return cases().filter(c=>c.lot===lot)}
  function summary(){const all=cases(),captured=all.filter(c=>c.referenceState==='CAPTURED_V147');return {...base.summary(),referenceVersion:VERSION,referenceCaptured:captured.length,referenceLinked:captured.reduce((n,c)=>n+c.referenceCoverage.linked,0),referenceExpected:captured.reduce((n,c)=>n+c.referenceCoverage.total,0),referenceIssues:captured.reduce((n,c)=>n+c.referenceIssues,0),declaredNonCanonical:captured.reduce((n,c)=>n+c.declaredReferenceRows.length,0),legacyReferenceNotCaptured:all.length-captured.length,integrity:`${base.summary().integrity} · ${INTEGRITY}`}}
  function openReference(caseId){const c=base.cases().find(x=>x.id===caseId);if(!c)return;openModal('COMERCIAL · REFERENCIAS V147','Validar relaciones internas verificables',`<div class="fields"><input type="hidden" name="sourceSchema" value="${base.schema}"><input type="hidden" name="referenceVersion" value="${VERSION}"><input type="hidden" name="caseId" value="${esc(c.id)}"><label>Caso<input value="${esc(c.id)} · ${esc(c.lot)}" readonly></label><label>Responsable humano<input name="reviewer" value="${esc(identity?.displayName||'Responsable DEMO')}" required></label><label class="full">Nota<textarea name="detail" placeholder="Activa validación V147 de supports, deliveryRef y referencias cruzadas Harvest/Economics."></textarea></label><label class="full">Frontera<input value="REFERENCE ≠ VERIFIED CONTRACT/PAYMENT/SALE · BUYER REF ≠ VERIFIED IDENTITY" readonly></label></div>`,true,'commercial-reference-meta')}
  function panel(){const all=cases(),captured=all.filter(c=>c.referenceState==='CAPTURED_V147'),linked=captured.reduce((n,c)=>n+c.referenceCoverage.linked,0),total=captured.reduce((n,c)=>n+c.referenceCoverage.total,0),issues=captured.reduce((n,c)=>n+c.referenceIssues,0),declared=captured.reduce((n,c)=>n+c.declaredReferenceRows.length,0);return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">COMMERCIAL · REFERENCIAS V147</p><h2>Relaciones verificables sin fabricar contrato, venta o recaudo</h2><p>Supports, entrega↔Harvest y referencias cruzadas Harvest/Economics se validan por destino. Comprador, acuerdo, precio, factura, pago y recaudo permanecen declarados no canónicos.</p></div><span class="status ${issues?'warn':'teal'}">${linked}/${total||0} REF</span></div><div class="card-body"><div class="grid metrics">${metric('Capturados V147',captured.length,'opt-in explícito')}${metric('Enlazadas',linked,'destino interno existente')}${metric('Issues',issues,'integridad referencial',issues?'warn':'good')}${metric('Refs. declaradas',declared,'fuera del denominador')}</div>${all.map(c=>`<div class="gate" style="margin-top:10px"><i class="${c.referenceState==='CAPTURED_V147'?(c.referenceIssues?'warn':'ok'):''}">${c.referenceState==='CAPTURED_V147'?(c.referenceIssues?'!':'✓'):'·'}</i><div><strong>${esc(c.id)} · ${esc(c.lot)}</strong><p>${c.referenceState==='CAPTURED_V147'?`${c.referenceCoverage.linked}/${c.referenceCoverage.total} internas · ${c.referenceIssues} issue(s) · ${c.declaredReferenceRows.length} declaradas no canónicas`:'LEGACY_REFERENCE_NOT_CAPTURED · no inválida'}</p></div><button class="btn secondary" data-commercial-ref="${esc(c.id)}">Validar referencias</button></div>`).join('')}<div class="section-note" style="margin-top:12px">BUYER REF ≠ VERIFIED IDENTITY · AGREEMENT/PRICE/INVOICE/PAYMENT/RECEIPT DECLARED ≠ VERIFIED/EXECUTED · CROSS-DOMAIN LINK ≠ COMMERCIAL EXECUTION · NO GUARANTEED REVENUE/CREDIT/INVESTMENT SIGNAL.</div></div></section>`}
  function insert(html,section){const markers=['<footer class="footer-note">','<footer class="footer">'];for(const m of markers){const i=html.lastIndexOf(m);if(i>=0)return html.slice(0,i)+section+html.slice(i)}return html+section}
  const priorResults=views.results;if(priorResults)views.results=()=>insert(priorResults(),panel());
  document.addEventListener('click',e=>{const b=e.target.closest('[data-commercial-ref]');if(b)openReference(b.dataset.commercialRef)});
  window.__SANA_COMMERCIAL_LEDGER__=Object.freeze({...base,referenceVersion:VERSION,cases,forLot,forCase:caseFor,summary,integrity:`${base.integrity} · ${INTEGRITY}`});
})();
