(() => {
  'use strict';
  const base=window.__SANA_CAPITAL_GOVERNANCE__;
  if(!base?.cases||base.schema!=='SANA_CAPITAL_GOVERNANCE_LEDGER_V1')return;
  const VERSION='V149';
  const TARGET_KINDS=new Set(['CAPITAL_NEED_DECLARED','USE_OF_FUNDS_DECLARED','DOCUMENT_COMPLETENESS_REVIEW','COUNTERPARTY_INTEREST','DUE_DILIGENCE_REQUEST','TERM_SHEET_REFERENCE','HUMAN_DECISION_REFERENCE','COMMITMENT_REFERENCE','CLOSING_STATUS_DECLARED','FUNDING_STATUS_DECLARED']);
  const INTEGRITY='CAPITAL_SUPPORT_REFERENCE ≠ VERIFIED_FACT · COUNTERPARTY_REF_DECLARED ≠ VERIFIED_IDENTITY · DD_REQUEST_REF_DECLARED ≠ APPROVAL · TERM_SHEET_REF_DECLARED ≠ SIGNED_OR_VERIFIED_TERM_SHEET · DECISION_REF_DECLARED ≠ APPROVAL_OR_EXECUTION · COMMITMENT_REF_DECLARED ≠ VERIFIED_COMMITMENT_OR_FUNDING · CLOSING_REF_DECLARED ≠ VERIFIED_LEGAL_CLOSE · FUNDING_REF_DECLARED ≠ DISBURSEMENT · EVIDENCE_REF_DECLARED ≠ VERIFIED_DOCUMENT · DOCUMENT_COMPLETENESS ≠ ELIGIBILITY ≠ CREDIT_SCORE · REFERENCE ≠ OFFER ≠ SOLICITATION ≠ BROKERAGE ≠ CUSTODY ≠ INVESTMENT_RECOMMENDATION';
  function metadata(){const out=new Map();for(const r of storage?.records||[]){if(r.type!=='capital-governance-reference-meta'||r.values?.sourceSchema!==base.schema)continue;const id=r.values?.caseId||'';if(id)out.set(id,{referenceVersion:r.values?.referenceVersion||'',recordId:r.id||''})}return out}
  function temporal(source,target){const a=Date.parse(source?.observedAt||''),b=Date.parse(target?.observedAt||'');return Number.isFinite(a)&&Number.isFinite(b)&&b>a?'FORWARD_REFERENCE':null}
  function resolveSupport(c,e,ref,all){if(!ref)return {status:'MISSING_REFERENCE',domain:'CAPITAL_GOVERNANCE_EVENT',target:null};let target=null,owner=null;for(const x of all){const t=(x.events||[]).find(ev=>ev.id===ref);if(t){target=t;owner=x;break}}if(!target)return {status:'MISSING_TARGET',domain:'CAPITAL_GOVERNANCE_EVENT',target:null};if(owner?.id!==c.id)return {status:'CROSS_CASE_REFERENCE',domain:'CAPITAL_GOVERNANCE_EVENT',target};if(!TARGET_KINDS.has(target.kind))return {status:'KIND_MISMATCH',domain:'CAPITAL_GOVERNANCE_EVENT',target};const time=temporal(e,target);if(time)return {status:time,domain:'CAPITAL_GOVERNANCE_EVENT',target};return {status:'LINKED',domain:'CAPITAL_GOVERNANCE_EVENT',target}}
  function canonicalRows(c,all){if(metadata().get(c.id)?.referenceVersion!==VERSION)return [];const rows=[];for(const e of c.events||[]){if(e.kind!=='EVIDENCE')continue;const refs=(e.supports||[]).length?e.supports:[''];for(const ref of refs)rows.push({sourceEventId:e.id,sourceKind:e.kind,kind:'CAPITAL_SUPPORT_REF',refId:ref||'',reference:resolveSupport(c,e,ref,all)})}return rows}
  function declaredRows(c){const out=[];const fields=[['counterpartyRef','COUNTERPARTY_REF_DECLARED'],['requestRef','DD_REQUEST_REF_DECLARED'],['termSheetRef','TERM_SHEET_REF_DECLARED'],['decisionRef','DECISION_REF_DECLARED'],['commitmentRef','COMMITMENT_REF_DECLARED'],['closingRef','CLOSING_REF_DECLARED'],['fundingRef','FUNDING_REF_DECLARED'],['evidenceRef','EVIDENCE_REF_DECLARED']];for(const e of c.events||[])for(const [field,kind] of fields)if(e[field]!==undefined&&e[field]!==null&&e[field]!=='')out.push({sourceEventId:e.id,kind,refId:String(e[field]),status:'DECLARED_NON_CANONICAL_REFERENCE'});return out}
  function caseFor(id){const all=base.cases(),c=all.find(x=>x.id===id);if(!c)return null;const meta=metadata().get(id)||{},captured=meta.referenceVersion===VERSION,rows=canonicalRows(c,all),linked=rows.filter(r=>r.reference.status==='LINKED').length;return {...c,referenceVersion:meta.referenceVersion||'',referenceState:captured?'CAPTURED_V149':'LEGACY_REFERENCE_NOT_CAPTURED',referenceCoverage:{linked,total:rows.length,percent:rows.length?Math.round(linked/rows.length*100):null},referenceIssues:rows.length-linked,referenceRows:rows,declaredReferenceRows:declaredRows(c),integrity:`${c.integrity} · ${INTEGRITY}`}}
  function cases(){return base.cases().map(c=>caseFor(c.id)).filter(Boolean)}
  function forLot(lot){return cases().filter(c=>c.lot===lot)}
  function summary(){const all=cases(),captured=all.filter(c=>c.referenceState==='CAPTURED_V149');return {...base.summary(),referenceVersion:VERSION,referenceCaptured:captured.length,referenceLinked:captured.reduce((n,c)=>n+c.referenceCoverage.linked,0),referenceExpected:captured.reduce((n,c)=>n+c.referenceCoverage.total,0),referenceIssues:captured.reduce((n,c)=>n+c.referenceIssues,0),declaredNonCanonical:captured.reduce((n,c)=>n+c.declaredReferenceRows.length,0),legacyReferenceNotCaptured:all.length-captured.length,integrity:`${base.summary().integrity} · ${INTEGRITY}`}}
  function openReference(caseId){const c=base.cases().find(x=>x.id===caseId);if(!c)return;openModal('CAPITAL GOVERNANCE · REFERENCIAS V149','Validar soportes internos del caso',`<div class="fields"><input type="hidden" name="sourceSchema" value="${base.schema}"><input type="hidden" name="referenceVersion" value="${VERSION}"><input type="hidden" name="caseId" value="${esc(c.id)}"><label>Caso<input value="${esc(c.id)} · ${esc(c.lot||'unidad')}" readonly></label><label>Responsable humano<input name="reviewer" value="${esc(identity?.displayName||'Responsable DEMO')}" required></label><label class="full">Nota<textarea name="detail" placeholder="Activa validación V149 de EVIDENCE.supports contra eventos previos del mismo caso."></textarea></label><label class="full">Frontera<input value="REFERENCE ≠ VERIFIED FACT/IDENTITY/CONTRACT/APPROVAL/FUNDING · NO SCORE/ELIGIBILITY/INVESTMENT AUTHORITY" readonly></label></div>`,true,'capital-governance-reference-meta')}
  function panel(){const all=cases(),captured=all.filter(c=>c.referenceState==='CAPTURED_V149'),linked=captured.reduce((n,c)=>n+c.referenceCoverage.linked,0),total=captured.reduce((n,c)=>n+c.referenceCoverage.total,0),issues=captured.reduce((n,c)=>n+c.referenceIssues,0),declared=captured.reduce((n,c)=>n+c.declaredReferenceRows.length,0);return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">CAPITAL GOVERNANCE · REFERENCIAS V149</p><h2>Soportes internos verificables sin fabricar aprobación, elegibilidad o funding</h2><p>Solo EVIDENCE.supports entra al denominador. Contraparte, DD request, term sheet, decisión, commitment, cierre, funding y evidencia externa permanecen declarados no canónicos.</p></div><span class="status ${issues?'warn':'teal'}">${linked}/${total||0} REF</span></div><div class="card-body"><div class="grid metrics">${metric('Capturados V149',captured.length,'opt-in explícito')}${metric('Enlazadas',linked,'evento previo mismo caso')}${metric('Issues',issues,'integridad referencial',issues?'warn':'good')}${metric('Refs. declaradas',declared,'fuera del denominador')}</div>${all.map(c=>`<div class="gate" style="margin-top:10px"><i class="${c.referenceState==='CAPTURED_V149'?(c.referenceIssues?'warn':'ok'):''}">${c.referenceState==='CAPTURED_V149'?(c.referenceIssues?'!':'✓'):'·'}</i><div><strong>${esc(c.id)} · ${esc(c.lot||'unidad')}</strong><p>${c.referenceState==='CAPTURED_V149'?`${c.referenceCoverage.linked}/${c.referenceCoverage.total} soporte(s) · ${c.referenceIssues} issue(s) · ${c.declaredReferenceRows.length} declaradas no canónicas`:'LEGACY_REFERENCE_NOT_CAPTURED · no inválida'}</p></div><button class="btn secondary" data-capital-governance-ref="${esc(c.id)}">Validar referencias</button></div>`).join('')}<div class="section-note" style="margin-top:12px">COUNTERPARTY/DD/TERM/DECISION/COMMITMENT/CLOSING/FUNDING REFS DECLARED ≠ VERIFIED/EXECUTED · DOCUMENT COMPLETENESS ≠ ELIGIBILITY/CREDIT SCORE · NO OFFER/SOLICITATION/BROKERAGE/CUSTODY/INVESTMENT RECOMMENDATION.</div></div></section>`}
  function insert(html,section){for(const marker of ['<footer class="footer-note">','<footer class="footer">']){const at=html.lastIndexOf(marker);if(at>=0)return html.slice(0,at)+section+html.slice(at)}return html+section}
  const priorCapital=views.capital;if(priorCapital)views.capital=()=>insert(priorCapital(),panel());
  document.addEventListener('click',e=>{const b=e.target.closest('[data-capital-governance-ref]');if(b)openReference(b.dataset.capitalGovernanceRef)});
  window.__SANA_CAPITAL_GOVERNANCE__=Object.freeze({...base,referenceVersion:VERSION,cases,forLot,forCase:caseFor,summary,integrity:`${base.integrity} · ${INTEGRITY}`});
})();
