(() => {'use strict';const SCHEMA='SANA_DUE_DILIGENCE_SNAPSHOT_V1',INTEGRITY='SNAPSHOT_DATAROOM_REVIEW_ROUND_ONLY · NO_LIVE_FALLBACK · LIVE_STATE ≠ SNAPSHOT ≠ HISTORICAL_EVIDENCE · ROUND_CHANGE ≠ REVIEW_PROGRESS ≠ CAPITAL_REVIEW_OUTCOME · ROUND_CLOSED ≠ REVIEW_COMPLETED ≠ DUE_DILIGENCE_APPROVAL · REVIEW_ONLY';const snaps=()=>((window.__SANA_DUE_DILIGENCE_SNAPSHOT__?.snapshots?.()||[]).filter(s=>s?.manifest?.schema===SCHEMA&&s?.reportType==='RPT-DD').sort((a,b)=>String(b.cutoff||'').localeCompare(String(a.cutoff||''))));const rows=m=>Array.isArray(m?.dataroomReviewRound?.rows)?m.dataroomReviewRound.rows:[];function state(){const s=snaps()[0];return !s?{valid:false,state:'NO_SNAPSHOT'}:{valid:true,state:s.manifest?.dataroomReviewRound?'CAPTURED':'NOT_CAPTURED_IN_SNAPSHOT',snapshot:s,rows:rows(s.manifest),integrity:INTEGRITY}}function diff(a,b){if(a?.manifest?.schema!==SCHEMA||b?.manifest?.schema!==SCHEMA)return{valid:false,changes:[]};const A=new Map(rows(a.manifest).map(r=>[r.roundId,r])),B=new Map(rows(b.manifest).map(r=>[r.roundId,r])),changes=[];for(const id of new Set([...A.keys(),...B.keys()])){const x=A.get(id),y=B.get(id);if(!x||!y){changes.push({entity:id,field:'Round presence',before:x?'PRESENTE':'—',after:y?'PRESENTE':'—'});continue}for(const f of ['cycleCount','statusCount','closureCount','roundStatuses'])if(JSON.stringify(x[f])!==JSON.stringify(y[f]))changes.push({entity:id,field:f,before:JSON.stringify(x[f]),after:JSON.stringify(y[f])})}return{valid:true,changes,total:changes.length,integrity:INTEGRITY}}window.__SANA_DATAROOM_REVIEW_ROUND_HISTORY__=Object.freeze({state,diff,rows,integrity:INTEGRITY});})();
