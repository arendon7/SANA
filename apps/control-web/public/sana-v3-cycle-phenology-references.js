(() => {
  'use strict';
  function forPlan(planId){const plan=DEMO.plans.find(p=>p.id===planId);if(!plan)return {valid:false};const api=window.__SANA_PHENOLOGY_SERIES__;const c=api?.referenceCoverage?.(plan.lot)||null;return {valid:true,plan:{id:plan.id,version:plan.version,lot:plan.lot},referenceCoverage:c?{linked:c.linked,total:c.total,percent:c.percent,issues:c.issues}:null,integrity:'PHENOLOGY_REFERENCE_PROVENANCE ≠ PLAN_PHASE ≠ CYCLE_GATE · REFERENCE ≠ MANAGEMENT_DECISION · REFERENCE ≠ CAUSALITY'}}
  function selected(){const p=window.__SANA_CYCLE_CLOSURE__?.selectedPlan?.();return p?forPlan(p.id):{valid:false}}
  function panel(){const s=selected();if(!s.valid)return '';const c=s.referenceCoverage;if(!c||!c.total)return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">CIERRE · REFERENCIAS FENOLOGÍA</p><h2>Sin referencias V133 en el ciclo</h2><p>La ausencia de granularidad referencial no cambia gates del cierre.</p></div><span class="status">NO PONDERADO</span></div></section>`;return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">CIERRE · REFERENCIAS FENOLOGÍA</p><h2>Integridad documental read-only</h2><p>${esc(s.plan.id)} · lote ${esc(s.plan.lot)} · ${c.linked}/${c.total} referencias enlazadas.</p></div><span class="status ${c.issues?'warn':'teal'}">${c.issues} ISSUE</span></div><div class="card-body"><div class="section-note">No modifica completeness ni readyForArchive. PHENOLOGY_REFERENCE_PROVENANCE ≠ PLAN_PHASE ≠ CYCLE_GATE · REFERENCE ≠ MANAGEMENT_DECISION ≠ CAUSALITY.</div></div></section>`}
  function insert(html,section){const marker='<footer class="footer">',at=html.lastIndexOf(marker);return at<0?html+section:html.slice(0,at)+section+html.slice(at)}
  const base=views.cycle;if(base)views.cycle=()=>insert(base(),panel());
  window.__SANA_CYCLE_PHENOLOGY_REFERENCES__=Object.freeze({forPlan,selected,integrity:'READ_ONLY · NON_WEIGHTED · PHENOLOGY_REFERENCE_PROVENANCE ≠ PLAN_PHASE ≠ CYCLE_GATE · REFERENCE ≠ MANAGEMENT_DECISION ≠ CAUSALITY'});
})();
