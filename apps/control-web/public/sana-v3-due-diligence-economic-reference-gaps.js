(() => {
  'use strict';
  const SCHEMA='SANA_DUE_DILIGENCE_SNAPSHOT_V1';
  const DOMAIN='Economía / referencias';
  const OWNER='Operaciones + Finanzas + Técnico';
  const ORDER={ALTA:3,MEDIA:2,BAJA:1};
  const INTEGRITY='SNAPSHOT_ECONOMIC_REFERENCE_GAPS_ONLY · NOT_CAPTURED_OR_LEGACY ≠ GAP · DECLARED_NON_CANONICAL_REFERENCE ≠ GAP · REFERENCE_ISSUE ≠ ACCOUNTING_ERROR ≠ FINANCIAL_DISTRESS ≠ CREDIT_RISK ≠ ELIGIBILITY ≠ INVESTMENT_RECOMMENDATION';
  function gap(id,entity,condition,source,severity='MEDIA',detail=''){return {id,domain:DOMAIN,entity,condition,source,severity,owner:OWNER,detail,status:'OPEN_AT_SNAPSHOT'}}
  function derive(snapshot){if(!snapshot||snapshot.manifest?.schema!==SCHEMA)return [];const d=snapshot.manifest?.economicReferences;if(!d)return [];const out=[];if(Number(d.contentLeakCount)>0)out.push(gap('economic-ref:content-leak',snapshot.manifest?.farm?.id||'Unidad',`${d.contentLeakCount} campo(s) de payload contable/no canónico detectados en granularidad referencial`,'Economic Reference Snapshot V146','ALTA','La proyección histórica debe contener solo estructura referencial minimizada y conteos no canónicos.'));for(const c of d.cases||[]){if(c.referenceState!=='CAPTURED_V145')continue;for(const r of c.rows||[]){if(r.status==='LINKED')continue;const sev=['MISSING_REFERENCE','MISSING_TARGET','CROSS_SCOPE_REFERENCE','CROSS_CASE_REFERENCE'].includes(r.status)?'ALTA':'MEDIA';out.push(gap(`economic-ref:${c.caseId}:${r.sourceEventId}:${r.kind}:${r.refId||'missing'}`,`${c.caseId||'SIN_CASO'} · ${c.lot||'SIN_LOTE'}`,`${r.kind||'REFERENCE'} · ${r.status||'INVALID_REFERENCE'}`,'Economic Reference Snapshot V146',sev,`ref=${r.refId||'—'} · domain=${r.domain||r.sourceDomain||'—'}. La inconsistencia no prueba error contable, pago, venta, caja ni riesgo financiero.`))}}return out}
  function merge(baseState,snapshot){if(!baseState?.valid)return baseState;const extra=derive(snapshot||baseState.snapshot),gaps=[...(baseState.gaps||[]),...extra];gaps.sort((a,b)=>(ORDER[b.severity]||0)-(ORDER[a.severity]||0)||String(a.domain).localeCompare(String(b.domain))||String(a.entity).localeCompare(String(b.entity)));const counts={ALTA:0,MEDIA:0,BAJA:0};gaps.forEach(g=>counts[g.severity]=(counts[g.severity]||0)+1);return {...baseState,gaps,counts,domains:[...new Set(gaps.map(g=>g.domain))],integrity:`${baseState.integrity||'SNAPSHOT_GAPS_ONLY'} · ${INTEGRITY}`}}
  const base=window.__SANA_DUE_DILIGENCE_GAPS__;
  if(base?.derive&&base?.current)window.__SANA_DUE_DILIGENCE_GAPS__=Object.freeze({schema:base.schema,latest:base.latest,derive:s=>merge(base.derive(s),s),current:()=>{const s=base.latest?.();return merge(base.derive(s),s)},economicReferenceGaps:derive,integrity:INTEGRITY});
  function panel(){const s=window.__SANA_DUE_DILIGENCE_GAPS__?.current?.();if(!s?.valid)return '';const list=(s.gaps||[]).filter(g=>g.domain===DOMAIN);if(!list.length)return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">BRECHAS · ECONOMIC REFERENCES</p><h2>Sin inconsistencias referenciales V146 capturadas</h2><p>Ausencia legacy/no capturada y referencias declaradas no canónicas no son gap por sí solas.</p></div><span class="status teal">0</span></div></section>`;return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">BRECHAS · ECONOMIC REFERENCES</p><h2>Integridad referencial económica que requiere atención</h2><p>${list.length} inconsistencia(s) estructural(es). No se puntúa solvencia, rentabilidad, pago, venta o riesgo crediticio.</p></div><span class="status warn">${list.length}</span></div><div class="card-body">${list.map(g=>`<div class="gate"><i class="${g.severity==='ALTA'?'blocked':'warn'}">${g.severity==='ALTA'?'!':'·'}</i><div><strong>${esc(g.entity)} · ${esc(g.condition)}</strong><p>${esc(g.source)}${g.detail?` · ${esc(g.detail)}`:''}</p></div><span class="status ${g.severity==='ALTA'?'danger':'warn'}">${esc(g.severity)}</span></div>`).join('')}<div class="section-note">NOT_CAPTURED_OR_LEGACY ≠ GAP · DECLARED_NON_CANONICAL_REFERENCE ≠ GAP · REFERENCE ISSUE ≠ ACCOUNTING ERROR ≠ PAYMENT/SALE/CASH FAILURE ≠ CREDIT/INVESTMENT SIGNAL.</div></div></section>`}
  function insert(html,section){const marker='<footer class="footer">';const i=html.lastIndexOf(marker);return i<0?html+section:html.slice(0,i)+section+html.slice(i)}
  const baseReports=views.reports;if(baseReports)views.reports=()=>insert(baseReports(),panel());
  window.__SANA_DD_ECONOMIC_REFERENCE_GAPS__=Object.freeze({derive,integrity:INTEGRITY});
})();
