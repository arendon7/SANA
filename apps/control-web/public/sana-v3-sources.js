(() => {
  'use strict';

  const BASE_SOURCES=[
    {id:'SRC-001',provider:'SHAREPOINT',name:'Protocolo técnico de cultivo DEMO',scope:'CAF-A1',version:'v3',cut:'2026-08-10',reviewer:'Laura Mejía',state:'REFERENCE_ONLY',externalId:'SP-DEMO-PROTO-001'},
    {id:'SRC-002',provider:'SHAREPOINT',name:'Ficha técnica de insumo DEMO',scope:'AGU-A2',version:'v2',cut:'2026-08-08',reviewer:'Camila Torres',state:'REFERENCE_ONLY',externalId:'SP-DEMO-INPUT-002'},
    {id:'SRC-003',provider:'SHAREPOINT',name:'Acta de visita agronómica DEMO',scope:'CAC-B1',version:'v1',cut:'2026-08-13',reviewer:'Laura Mejía',state:'REFERENCE_ONLY',externalId:'SP-DEMO-VISIT-003'},
    {id:'SRC-004',provider:'SHAREPOINT',name:'Línea base predial DEMO',scope:'FIN-LE-001',version:'v4',cut:'2026-08-01',reviewer:'Marta Restrepo',state:'REFERENCE_ONLY',externalId:'SP-DEMO-BASE-004'}
  ];

  const roleRaw=String(identity?.role||'').toLowerCase();
  const canCreate=/admin|technical|técn|producer|productor/.test(roleRaw);
  function localSources(){return storage.records.filter(r=>r.type==='document-source-link').map(r=>({
    id:r.id,provider:'SHAREPOINT',name:r.values?.name||r.title||'Fuente documental DEMO',scope:r.values?.lot||r.lot||'FIN-LE-001',version:r.values?.version||'sin versión',cut:r.values?.cut||String(r.createdAt||'').slice(0,10),reviewer:r.values?.reviewer||identity?.displayName||'Usuario DEMO',state:'REFERENCE_ONLY',externalId:r.values?.externalId||'REF-DEMO',local:true,notes:r.values?.detail||''
  })).reverse()}
  function rows(){return [...localSources(),...BASE_SOURCES]}
  function scopes(){return new Set(rows().map(r=>r.scope)).size}
  function forScope(scope){return rows().filter(r=>r.scope===scope).map(r=>({...r}))}

  function sourcesView(){
    const list=rows(); const local=localSources();
    return `${head('SANA · FUENTES DOCUMENTALES','Procedencia antes que apariencia de certeza.','Esta capa registra metadatos de documentos externos —incluido SharePoint— para saber qué fuente, versión, alcance, corte y revisor sustentan una lectura. La DEMO no lleva credenciales Microsoft al navegador, no modifica SharePoint y no convierte una referencia en verificación.',canCreate?'<button class="btn primary" data-document-source>Vincular referencia DEMO</button>':'')}
      <section class="grid metrics">${metric('Fuentes registradas',list.length,'base sintética + referencias de identidad','good')}${metric('Ámbitos cubiertos',scopes(),'predio / lotes DEMO','good')}${metric('Referencias propias',local.length,local.length?'LOCAL/Nube DEMO según sync':'sin referencias creadas',local.length?'good':'')}${metric('Credenciales en browser','0','sin token Microsoft / Graph','good')}</section>
      <article class="card"><div class="card-head"><div><h2>Source Registry</h2><p>Metadatos trazables; no copia documental ni ACL.</p></div><span class="status">REFERENCE_ONLY</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Fuente</th><th>Proveedor</th><th>Ámbito</th><th>Versión</th><th>Corte</th><th>Revisor</th><th>Referencia externa</th><th>Estado</th></tr></thead><tbody>${list.map(r=>`<tr><td><strong>${esc(r.name)}</strong>${r.local?'<br><small>Creada por esta identidad DEMO</small>':''}</td><td>${esc(r.provider)}</td><td>${esc(r.scope)}</td><td>${esc(r.version)}</td><td>${esc(r.cut)}</td><td>${esc(r.reviewer)}</td><td><code>${esc(r.externalId)}</code></td><td><span class="status warn">${esc(r.state)}</span></td></tr>`).join('')}</tbody></table></div></article>
      <section class="grid two" style="margin-top:14px"><article class="card"><div class="card-head"><div><h2>Contrato SharePoint → SANA</h2><p>Qué sí viaja y qué permanece fuera.</p></div></div><div class="card-body"><div class="flow"><div class="flow-step"><b>01</b><span>Identificador</span><small>referencia externa</small></div><div class="flow-step"><b>02</b><span>Versión</span><small>edición/corte</small></div><div class="flow-step"><b>03</b><span>Ámbito</span><small>predio/lote/caso</small></div><div class="flow-step"><b>04</b><span>Revisor</span><small>responsable humano</small></div><div class="flow-step"><b>05</b><span>Uso</span><small>evidencia/contexto</small></div></div><div class="section-note" style="margin-top:12px">El contenido completo, permisos, historial y escritura permanecen en SharePoint. Una integración productiva deberá resolverlos del lado servidor con identidad y autorización explícitas.</div></div></article><article class="card"><div class="card-head"><div><h2>Fronteras del navegador</h2><p>Previenen que “integrado” se confunda con “autorizado”.</p></div></div><div class="card-body"><div class="gate"><i class="blocked">×</i><div><strong>Token Microsoft / Graph</strong><p>No se almacena ni solicita en esta DEMO.</p></div><span class="status danger">0</span></div><div class="gate"><i class="blocked">×</i><div><strong>Modificar documento externo</strong><p>El browser no escribe, renombra ni elimina archivos.</p></div><span class="status danger">BLOQUEADO</span></div><div class="gate"><i class="warn">!</i><div><strong>Referencia documental</strong><p>Indica procedencia; requiere revisión para afirmar contenido.</p></div><span class="status warn">REFERENCE_ONLY</span></div><div class="gate"><i>✓</i><div><strong>Vincular al expediente</strong><p>Predio/lote puede mostrar qué fuente fue usada.</p></div><span class="status teal">DEMO</span></div></div></article></section>
      <section class="quick-grid" style="margin-top:14px"><button class="quick" data-view-link="territory"><strong>Territorio 360°</strong><span>Relaciona fuentes con el expediente espacial.</span></button><button class="quick" data-view-link="reports"><strong>Informes</strong><span>Fuentes, versión y fecha de corte.</span></button><button class="quick" data-view-link="passport"><strong>Passport</strong><span>Procedencia y cadena de evidencia.</span></button><button class="quick" data-view-link="capital"><strong>Readiness</strong><span>Documentación sin oferta ni transacción.</span></button></section>${footer()}`;
  }

  views.sources=sourcesView;
  window.__SANA_DOCUMENT_SOURCES__=Object.freeze({rows:()=>rows().map(r=>({...r})),forScope});

  function openSourceForm(){
    const scopeOptions=[`<option value="${DEMO.farm.id}">${DEMO.farm.id} · Predio</option>`,...DEMO.lots.map(l=>`<option value="${l.id}">${l.id} · ${esc(l.crop)}</option>`)].join('');
    openModal('FUENTE DOCUMENTAL · DEMO','Vincular referencia SharePoint',`<div class="fields"><label>Proveedor<input value="SHAREPOINT" readonly></label><label>Ámbito<select name="lot">${scopeOptions}</select></label><label class="full">Nombre de referencia<input name="name" required placeholder="Nombre descriptivo del documento"></label><label>Referencia externa<input name="externalId" required placeholder="ID / clave documental, no URL privada"></label><label>Versión<input name="version" required placeholder="v1 / edición / hash documental"></label><label>Fecha de corte<input name="cut" type="date" required></label><label>Revisor humano<input name="reviewer" value="${esc(identity?.displayName||'Responsable DEMO')}" required></label><label class="full">Uso / procedencia<textarea name="detail" placeholder="Qué lectura sustenta y qué debe verificarse"></textarea></label><label class="full">Integridad<input value="REFERENCE_ONLY · NO LEE NI MODIFICA SHAREPOINT · NO ES ACK PRODUCTIVO" readonly></label></div>`,true,'document-source-link');
  }
  document.addEventListener('click',event=>{if(event.target.closest('[data-document-source]'))openSourceForm()});
})();
