(() => {
  'use strict';
  const SCHEMA='SANA_DUE_DILIGENCE_SNAPSHOT_V1';
  const DOMAIN='Sanidad vegetal';
  const OWNER='Técnico + Productor';
  const SEVERITY_ORDER={ALTA:3,MEDIA:2,BAJA:1};

  function rows(manifest){const lots=Array.isArray(manifest?.health?.lots)?manifest.health.lots:[];return lots.flatMap(lot=>(lot.cases||[]).map(row=>({...row,lotId:lot.lotId||row.lot||''})))}
  function legacyRows(manifest){const lots=Array.isArray(manifest?.health?.lots)?manifest.health.lots:[];return lots.flatMap(lot=>(lot.legacy||[]).map(row=>({...row,lotId:lot.lotId||row.lot||''})))}
  function gap(id,entity,condition,source,severity='MEDIA',detail=''){return {id,domain:DOMAIN,entity,condition,source,severity,owner:OWNER,detail,status:'OPEN_AT_SNAPSHOT'}}
  function coverageOf(row){const hasV2=row.chainCoverage!==undefined&&row.chainCoverage!==null&&row.chainCoverage!=='';const v2=Number(row.chainCoverage);if(hasV2&&Number.isFinite(v2))return {value:v2,version:'V2',source:'Health Snapshot · chainCoverage'};const hasV1=row.stageCoverage!==undefined&&row.stageCoverage!==null&&row.stageCoverage!=='';const v1=Number(row.stageCoverage);return {value:hasV1&&Number.isFinite(v1)?v1:null,version:'V1',source:'Health Snapshot · stageCoverage'}}
  function referenceState(row){
    const captured=row.referenceExpectedCount!==undefined||row.referenceIssueCount!==undefined||row.referenceCoverage!==undefined||Array.isArray(row.referenceRows);if(!captured)return {captured:false,expected:0,linked:0,issues:0,coverage:null,statuses:[]};
    const expected=Number(row.referenceExpectedCount||0),linked=Number(row.referenceLinkedCount||0),issues=Number(row.referenceIssueCount||0);const hasCoverage=row.referenceCoverage!==undefined&&row.referenceCoverage!==null&&row.referenceCoverage!=='';const numeric=Number(row.referenceCoverage);const coverage=hasCoverage&&Number.isFinite(numeric)?numeric:null;const statuses=(Array.isArray(row.referenceRows)?row.referenceRows:[]).filter(x=>x?.status&&x.status!=='LINKED').map(x=>x.status);return {captured:true,expected,linked,issues,coverage,statuses};
  }
  function deriveHealth(snapshot){
    if(!snapshot||snapshot.manifest?.schema!==SCHEMA)return [];const manifest=snapshot.manifest||{};
    if(!manifest.health)return [gap('health:granularity',manifest.farm?.id||'Unidad','Granularidad del ledger sanitario no capturada en este snapshot','Snapshot manifest · health','BAJA','El corte es válido y puede ser anterior a esta capacidad. No se rellena desde Sanidad viva; requiere un nuevo snapshot.')];
    const out=[];const list=rows(manifest),legacy=legacyRows(manifest);
    if(!list.length&&legacy.length)out.push(gap('health:legacy-only',manifest.farm?.id||'Unidad','Solo existen resúmenes sanitarios legacy en el corte','Health Snapshot · legacy','BAJA','LEGACY_INCIDENT_SUMMARY ≠ OBSERVED_PRESENCE ≠ DIAGNOSIS ≠ TREATMENT ≠ EFFICACY.'));
    list.forEach(r=>{
      const entity=`${r.lotId||r.lot||'SIN_LOTE'} · ${r.caseId||'SIN_CASO'}`;const coverage=coverageOf(r),stage=coverage.value,refs=referenceState(r),evidence=Number(r.evidenceCount||0),actions=Number(r.actionCount||0),follow=Number(r.followUpCount||0),linkIssues=Number(r.actionLinkIssues||0),diagnosis=Number(r.confirmedDiagnosis||0),presence=Number(r.observedPresence||0),activityLinks=Number(r.activityLinkEventCount||0),embeddedActivityLinks=Number(r.embeddedActivityLinkCount||0),results=Number(r.resultEventCount||0),embeddedResults=Number(r.embeddedResultCount||0),explicitResultEfficacy=Number(r.explicitResultEfficacyObservationCount||0);
      if(Number.isFinite(stage)&&stage<50)out.push(gap(`health:${entity}:stages-high`,entity,`Cobertura de cadena sanitaria ${coverage.version} ${stage}%`,coverage.source,'ALTA',coverage.version==='V2'?'Prioridad documental V2; valores V1 embebidos no completan etapas V2. No evalúa severidad agronómica ni riesgo crediticio.':'Prioridad documental; no evalúa severidad agronómica ni riesgo crediticio.'));
      else if(Number.isFinite(stage)&&stage<80)out.push(gap(`health:${entity}:stages`,entity,`Cobertura de cadena sanitaria ${coverage.version} ${stage}%`,coverage.source,'MEDIA',coverage.version==='V2'?'Cobertura de procedencia V2; no equivale a desempeño sanitario. V1_EMBEDDED ≠ V2_STAGE.':''));
      if(refs.captured&&refs.expected>0&&refs.issues>0){const severity=refs.issues>=refs.expected?'ALTA':'MEDIA';const statusText=[...new Set(refs.statuses)].join(', ')||'referencias no enlazadas';out.push(gap(`health:${entity}:predecessor-reference`,entity,`${refs.issues} de ${refs.expected} referencia(s) V2 requieren revisión`,'Health Snapshot · referenceIssueCount/referenceRows',severity,`${statusText}. CASE_MEMBERSHIP ≠ PREDECESSOR_REFERENCE; REFERENCE ≠ CAUSALITY. No reconstruir referencias automáticamente.`))}
      if(linkIssues>0)out.push(gap(`health:${entity}:activity-link`,entity,`${linkIssues} vínculo(s) de acción con actividad inconsistente`,'Health Snapshot · actionLinkIssues','ALTA','Revisar activityId/lote. No corregir automáticamente ni afirmar ejecución si el vínculo es inconsistente.'));
      if(coverage.version==='V2'&&actions>0&&activityLinks===0&&embeddedActivityLinks>0)out.push(gap(`health:${entity}:activity-link-v2`,entity,'Relación con Activity disponible solo como vínculo embebido V1','Health Snapshot · activityLinkEventCount/embeddedActivityLinkCount','MEDIA','Existe procedencia histórica V1, pero falta ACTIVITY_LINK explícito V2. EMBEDDED_V1_RELATION ≠ V2_STAGE; vínculo ≠ ejecución.'));
      if(coverage.version==='V2'&&actions>0&&activityLinks===0&&embeddedActivityLinks===0)out.push(gap(`health:${entity}:activity-link-missing`,entity,'Acción registrada sin relación Activity explícita capturada','Health Snapshot · activityLinkEventCount/actionCount','MEDIA','Brecha de procedencia relacional. No implica que la acción no haya ocurrido ni autoriza reconstrucción automática.'));
      if(actions>0&&evidence===0)out.push(gap(`health:${entity}:action-evidence`,entity,'Acción sanitaria registrada sin evidencia capturada','Health Snapshot · actionCount/evidenceCount','ALTA','ACCIÓN REGISTRADA ≠ EFICACIA DEMOSTRADA.'));
      if(actions>0&&follow===0)out.push(gap(`health:${entity}:follow-up`,entity,'Acción sanitaria registrada sin seguimiento capturado','Health Snapshot · actionCount/followUpCount','MEDIA','No permite describir observaciones posteriores; tampoco implica ineficacia.'));
      if(coverage.version==='V2'&&follow>0&&results===0&&embeddedResults>0)out.push(gap(`health:${entity}:result-v2`,entity,'Resultado disponible solo como dato embebido V1 dentro de seguimiento','Health Snapshot · resultEventCount/embeddedResultCount','MEDIA','Existe contexto histórico V1, pero falta RESULT explícito V2. FOLLOW_UP ≠ RESULT; EMBEDDED_V1_RELATION ≠ V2_STAGE.'));
      if(coverage.version==='V2'&&follow>0&&results===0&&embeddedResults===0)out.push(gap(`health:${entity}:result-missing`,entity,'Seguimiento capturado sin evento RESULT explícito V2','Health Snapshot · followUpCount/resultEventCount','MEDIA','Seguimiento ≠ resultado. No se infiere resultado ni eficacia desde el seguimiento.'));
      if(coverage.version==='V2'&&results>0&&follow===0)out.push(gap(`health:${entity}:result-follow-up`,entity,'Resultado V2 capturado sin seguimiento explícito en la cadena','Health Snapshot · resultEventCount/followUpCount','MEDIA','Revisar procedencia temporal. RESULT ≠ CAUSAL_ATTRIBUTION y no demuestra secuencia clínica/agronómica por sí solo.'));
      if(diagnosis>0&&presence===0)out.push(gap(`health:${entity}:diagnosis-presence`,entity,'Diagnóstico humano confirmado sin presencia observada capturada en la cadena','Health Snapshot · confirmedDiagnosis/observedPresence','MEDIA','Puede existir otra base diagnóstica, pero el snapshot no contiene la observación explícita de presencia. No invalidar automáticamente el diagnóstico.'));
      if(coverage.version==='V2'&&explicitResultEfficacy>0&&actions===0)out.push(gap(`health:${entity}:efficacy-action-v2`,entity,'Observación de efecto en RESULT V2 sin acción registrada en la cadena capturada','Health Snapshot · explicitResultEfficacyObservationCount/actionCount','ALTA','Revisar procedencia. RESULT ≠ CAUSAL_ATTRIBUTION; una mejoría observada no demuestra eficacia del tratamiento.'));
      else if(Number(r.efficacyObservationCount||0)>0&&actions===0)out.push(gap(`health:${entity}:efficacy-action`,entity,'Observación de eficacia/efecto sin acción registrada en la cadena capturada','Health Snapshot · efficacyObservationCount/actionCount','ALTA','Revisar procedencia. FOLLOW_UP/RESULT ≠ CAUSAL_ATTRIBUTION.'));
    });
    return out;
  }
  function mergeState(baseState,snapshot){
    if(!baseState?.valid)return baseState;const health=deriveHealth(snapshot||baseState.snapshot);const gaps=[...(baseState.gaps||[]),...health];gaps.sort((a,b)=>(SEVERITY_ORDER[b.severity]||0)-(SEVERITY_ORDER[a.severity]||0)||String(a.domain).localeCompare(String(b.domain))||String(a.entity).localeCompare(String(b.entity)));const counts={ALTA:0,MEDIA:0,BAJA:0};gaps.forEach(g=>{counts[g.severity]=(counts[g.severity]||0)+1});return {...baseState,gaps,counts,domains:[...new Set(gaps.map(g=>g.domain))],integrity:'SNAPSHOT_GAPS_ONLY · DOCUMENTARY_PRIORITY ≠ AGRONOMIC_SEVERITY ≠ CREDIT_RISK ≠ ELIGIBILITY ≠ INVESTMENT_RECOMMENDATION · PHYTOSANITARY_PROVENANCE · CASE_MEMBERSHIP ≠ PREDECESSOR_REFERENCE · REFERENCE ≠ CAUSALITY · V1_EMBEDDED ≠ V2_STAGE'};
  }
  const base=window.__SANA_DUE_DILIGENCE_GAPS__;
  if(base?.derive&&base?.current){window.__SANA_DUE_DILIGENCE_GAPS__=Object.freeze({schema:base.schema,latest:base.latest,derive:snapshot=>mergeState(base.derive(snapshot),snapshot),current:()=>{const snapshot=base.latest?.();return mergeState(base.derive(snapshot),snapshot)},healthGaps:deriveHealth,integrity:'SNAPSHOT_GAPS_ONLY · DOCUMENTARY_PRIORITY ≠ AGRONOMIC_SEVERITY ≠ CREDIT_RISK ≠ ELIGIBILITY ≠ INVESTMENT_RECOMMENDATION · PHYTOSANITARY_PROVENANCE · CASE_MEMBERSHIP ≠ PREDECESSOR_REFERENCE · REFERENCE ≠ CAUSALITY · V1_EMBEDDED ≠ V2_STAGE'})}

  function panel(){
    const state=window.__SANA_DUE_DILIGENCE_GAPS__?.current?.();if(!state?.valid)return '';const health=(state.gaps||[]).filter(g=>g.domain===DOMAIN);
    if(!health.length)return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">BRECHAS · SANIDAD VEGETAL</p><h2>Sin brechas sanitarias documentales según este corte</h2><p>La lectura se limita a trazabilidad del snapshot. No equivale a ausencia de plagas/enfermedades, certificación o bajo riesgo.</p></div><span class="status teal">0</span></div></section>`;
    return `<section class="card" style="margin-top:14px"><div class="card-head"><div><p class="kicker">BRECHAS · SANIDAD VEGETAL</p><h2>Trazabilidad sanitaria que requiere atención documental</h2><p>${health.length} condición(es) del último snapshot. La presencia de enfermedad o una acción aplicada no constituyen por sí solas una brecha Due Diligence.</p></div><span class="status warn">${health.length}</span></div><div class="card-body">${health.map(g=>`<div class="gate"><i class="${g.severity==='ALTA'?'blocked':'warn'}">${g.severity==='ALTA'?'!':'·'}</i><div><strong>${esc(g.entity)} · ${esc(g.condition)}</strong><p>${esc(g.source)}${g.detail?` · ${esc(g.detail)}`:''}</p></div><span class="status ${g.severity==='ALTA'?'danger':g.severity==='MEDIA'?'warn':'teal'}">${esc(g.severity)}</span></div>`).join('')}<div class="section-note" style="margin-top:12px">HEALTH GAP ≠ DISEASE SEVERITY ≠ TREATMENT FAILURE ≠ CREDIT RISK ≠ INVESTMENT SIGNAL. CASE_MEMBERSHIP ≠ PREDECESSOR_REFERENCE. REFERENCE ≠ CAUSALITY. V1_EMBEDDED ≠ V2_STAGE. Solo un corte posterior puede demostrar cambio documental.</div></div></section>`;
  }
  function insertBeforeFooter(html,section){const marker='<footer class="footer">';const at=html.lastIndexOf(marker);return at<0?`${html}${section}`:`${html.slice(0,at)}${section}${html.slice(at)}`}
  const baseReports=views.reports;if(baseReports)views.reports=function reportsWithHealthGaps(){return insertBeforeFooter(baseReports(),panel())};
  window.__SANA_DD_HEALTH_GAPS__=Object.freeze({derive:deriveHealth,coverageOf,referenceState,integrity:'SNAPSHOT_HEALTH_GAPS_ONLY · NO_LIVE_FALLBACK · PRESENCE_OR_TREATMENT ≠ GAP · CASE_MEMBERSHIP ≠ PREDECESSOR_REFERENCE · REFERENCE ≠ CAUSALITY · V1_EMBEDDED ≠ V2_STAGE · NO_CAUSALITY_OR_CREDIT_INFERENCE'});
})();
