name: Assemble SANA v63 Capital Human Review

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  assemble-v63-retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v63 assembler retired after deterministic assembly; shell is certified separately.'
