name: Assemble SANA v65 Data Room Access Governance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  assemble-v65-retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v65 assembler retired after deterministic assembly; shell is certified separately.'
