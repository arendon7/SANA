name: Assemble SANA v59 Commercial Offtake

on:
  push:
    branches: [demo/sana-functional-auth-v1]
    paths:
      - 'apps/control-web/public/sana-v3-commercial-ledger.js'
      - 'apps/control-web/public/sana-v3-cycle-commercial-provenance.js'
      - 'scripts/validate-sana-commercial-ledger.mjs'
      - '.github/workflows/sana-demo-commercial-ledger.yml'
      - '.github/workflows/assemble-sana-v59.yml'

permissions:
  contents: write

jobs:
  assemble-v59:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: demo/sana-functional-auth-v1
          fetch-depth: 0
      - name: Assemble HTML and service worker deterministically
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          hp=Path('apps/control-web/public/sana-v3.html'); sp=Path('apps/control-web/public/sana-v3-sw.js')
          html=hp.read_text(); sw=sp.read_text()
          repl=[
            ('<script src="/sana-v3-economics-ledger.js"></script><script src="/sana-v3-material-chain.js"></script>',
             '<script src="/sana-v3-economics-ledger.js"></script><script src="/sana-v3-commercial-ledger.js"></script><script src="/sana-v3-material-chain.js"></script>'),
            ('<script src="/sana-v3-cycle-economic-reconciliation.js"></script><script src="/sana-v3-cycle-labor-provenance.js"></script>',
             '<script src="/sana-v3-cycle-economic-reconciliation.js"></script><script src="/sana-v3-cycle-commercial-provenance.js"></script><script src="/sana-v3-cycle-labor-provenance.js"></script>')]
          for old,new in repl:
              if html.count(old)!=1: raise SystemExit(f'HTML anchor count {html.count(old)}: {old}')
              html=html.replace(old,new,1)
          old="const CACHE='sana-v3-demo-shell-v58';"
          new="if(false){\nconst CACHE='sana-v3-demo-shell-v58';\n}\nconst CACHE='sana-v3-demo-shell-v59';"
          if sw.count(old)!=1: raise SystemExit(f'SW v58 anchor count {sw.count(old)}')
          sw=sw.replace(old,new,1)
          srep=[
            ("'/sana-v3-economics-ledger.js','/sana-v3-material-chain.js'","'/sana-v3-economics-ledger.js','/sana-v3-commercial-ledger.js','/sana-v3-material-chain.js'"),
            ("'/sana-v3-cycle-economic-reconciliation.js','/sana-v3-cycle-labor-provenance.js'","'/sana-v3-cycle-economic-reconciliation.js','/sana-v3-cycle-commercial-provenance.js','/sana-v3-cycle-labor-provenance.js'")]
          for old,new in srep:
              if sw.count(old)!=1: raise SystemExit(f'SW anchor count {sw.count(old)}: {old}')
              sw=sw.replace(old,new,1)
          hp.write_text(html); sp.write_text(sw)
          PY
          grep -q '/sana-v3-economics-ledger.js.*sana-v3-commercial-ledger.js.*sana-v3-material-chain.js' apps/control-web/public/sana-v3.html
          grep -q '/sana-v3-cycle-economic-reconciliation.js.*sana-v3-cycle-commercial-provenance.js.*sana-v3-cycle-labor-provenance.js' apps/control-web/public/sana-v3.html
          grep -q "^const CACHE='sana-v3-demo-shell-v59';" apps/control-web/public/sana-v3-sw.js
      - name: Commit assembled v59
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/control-web/public/sana-v3.html apps/control-web/public/sana-v3-sw.js
          git diff --cached --quiet && exit 0
          git commit -m 'build(demo): assemble commercial offtake shell v59'
          git push origin HEAD:demo/sana-functional-auth-v1
