name: Assemble SANA v56 Source Evidence History (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired after certified assembly
        run: |
          echo 'v56 was already assembled on the DEMO branch.'
          echo 'This workflow is retained only as an audit marker and performs no writes.'
