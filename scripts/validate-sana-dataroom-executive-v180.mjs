import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';

const paths={v171:'apps/control-web/public/sana-v3-dataroom-executive-v171.js',v172:'apps/control-web/public/sana-v3-dataroom-executive-v172.js',v173:'apps/control-web/public/sana-v3-dataroom-executive-v173.js',v174:'apps/control-web/public/sana-v3-dataroom-executive-v174.js',source175:'apps/control-web/public/sana-v3-dataroom-claim-attestation-v175.js',v175:'apps/control-web/public/sana-v3-dataroom-executive-v175.js',source176:'apps/control-web/public/sana-v3-dataroom-claim-external-verification-v176.js',v176:'apps/control-web/public/sana-v3-dataroom-executive-v176.js',source177:'apps/control-web/public/sana-v3-dataroom-claim-evidence-sufficiency-v177.js',v177:'apps/control-web/public/sana-v3-dataroom-executive-v177.js',source178:'apps/control-web/public/sana-v3-dataroom-claim-evidence-handoff-v178.js',v178:'apps/control-web/public/sana-v3-dataroom-executive-v178.js',source179:'apps/control-web/public/sana-v3-dataroom-claim-evidence-remediation-round-v179.js',v179:'apps/control-web/public/sana-v3-dataroom-executive-v179.js',source180:'apps/control-web/public/sana-v3-dataroom-claim-current-remediation-round-v180.js',v180:'apps/control-web/public/sana-v3-dataroom-executive-v180.js'};
const code=Object.fromEntries(Object.entries(paths).map(([k,p])=>[k,fs.readFileSync(p,'utf8')]));
for(const [pattern,label] of [[/VERSION='V180'/,'version'],[/SANA_DATAROOM_EXECUTIVE_EXACT_CURRENT_REMEDIATION_ROUND_POINTER_V1/,'schema'],[/PARENT_SHA='01b48db1a035256ad82ec58bfe8f69b318240f63'/,'parent sha'],[/CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_EXACT_V1/,'typed pointer rule'],[/EXACTLY_ONE_POINTER_CANDIDATE_PER_CLAIM/,'unique pointer'],[/EXACT_V179_ROUND_REF/,'exact parent round match'],[/NO_TIMESTAMP_SOURCE_ORDER_OR_REF_ORDER_INFERENCE/,'no time/order inference'],[/currentRoundAuthority:false/,'current authority false'],[/latestRoundAuthority:false/,'latest authority false'],[/chronologyAuthority:false/,'chronology authority false'],[/COUNTS_ONLY ≠ SCORE/,'score boundary']])assert.ok(pattern.test(code.v180),`missing V180 executive invariant: ${label}`);
for(const [pattern,label] of [[/SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_V1/,'source schema'],[/CURRENT_ROUND_REFERENCE_ONLY/,'state'],[/CURRENT_ROUND_REF_REQUIRED/,'round required'],[/CURRENT_ROUND_POINTER_STATE_NOT_ALLOWED/,'state rejection'],[/DUPLICATE_POINTER_REF/,'duplicate ref rejection'],[/MULTIPLE_CURRENT_POINTERS_FOR_CLAIM/,'multiple pointer rejection'],[/ONE_ACCEPTED_POINTER_PER_CLAIM_ENVELOPE/,'one pointer invariant']])assert.ok(pattern.test(code.source180),`missing V180 source invariant: ${label}`);
for(const src of [code.source180,code.v180])for(const forbidden of ['fetch(','XMLHttpRequest','localStorage.setItem','sessionStorage.setItem','indexedDB.open','canonicalMutationAvailable:true','financialMutationAvailable:true','currentRoundAuthority:true','latestRoundAuthority:true','chronologyAuthority:true','claimTruthAuthority:true','evidenceAcceptanceAuthority:true','evidentiarySufficiencyAuthority:true','findingResolutionAuthority:true','dueDiligenceApprovalAuthority:true','eligibilityAuthority:true','financingApprovalAuthority:true','decisionAuthority:true','riskScore:','creditScore:','investmentScore:','projectScore:','overallScore:'])assert.ok(!src.includes(forbidden),`forbidden V180 token: ${forbidden}`);

const context={window:{},structuredClone,console};context.globalThis=context;for(const key of Object.keys(paths))vm.runInNewContext(code[key],context,{filename:paths[key]});
const attFactory=context.window.__SANA_DATAROOM_CLAIM_ATTESTATION_V175_FACTORY__,extFactory=context.window.__SANA_DATAROOM_CLAIM_EXTERNAL_VERIFICATION_V176_FACTORY__,reviewFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_SUFFICIENCY_V177_FACTORY__,handoffFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_HANDOFF_V178_FACTORY__,roundFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V179_FACTORY__,pointerFactory=context.window.__SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_V180_FACTORY__,execFactory=context.window.__SANA_DATAROOM_EXECUTIVE_V180_FACTORY__;
assert.equal(pointerFactory.schema,'SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_V1');assert.equal(pointerFactory.state,'CURRENT_ROUND_REFERENCE_ONLY');assert.equal(context.window.__SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND__.records().length,0);assert.equal(execFactory.version,'V180');

const data={snapshots:[{id:'SNAP-A',reportType:'RPT-DD',cutoff:'2026-08-20',manifest:{schema:'SANA_DUE_DILIGENCE_SNAPSHOT_V1'}}],state:{valid:true,state:'CAPTURED',snapshot:{id:'SNAP-A',cutoff:'2026-08-20'},rows:[{id:'ROW-A',lotId:'LOT-A'},{id:'ROW-B',lotId:'LOT-B'}],integrity:'SNAPSHOT_ONLY'},capital:[{id:'CAP-A',lot:'LOT-A',events:[{id:'CAP-A-E1',lot:'LOT-A',kind:'REVIEW_STARTED'}]},{id:'CAP-B',lot:'LOT-B',events:[{id:'CAP-B-E1',lot:'LOT-B',kind:'REVIEW_STARTED'}]}],refs:[{id:'RG-A',lot:'LOT-A',events:[{id:'RG-A-E1',lot:'LOT-A'}]},{id:'RG-B',lot:'LOT-B',events:[{id:'RG-B-E1',lot:'LOT-B'}]}]};const state=()=>data.state;
function baseHost(){return {DEMO:{lots:[{id:'LOT-A'},{id:'LOT-B'}]},__SANA_DATAROOM_360__:{state},__SANA_DUE_DILIGENCE_SNAPSHOT__:{snapshots:()=>data.snapshots},__SANA_CAPITAL_REVIEW__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_CAPITAL_GOVERNANCE__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_DATAROOM_FINDINGS__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_DUE_DILIGENCE_GAPS__:{current:()=>({valid:true,state:'CAPTURED',snapshot:data.state.snapshot,gaps:[{id:'GAP-A',lot:'LOT-A'},{id:'GAP-B',lot:'LOT-B'}]})},__SANA_DATAROOM_PHENOLOGY_HISTORY__:{state},__SANA_DATAROOM_LABOR_HISTORY__:{state},__SANA_DATAROOM_HEALTH_HISTORY__:{state},__SANA_DATAROOM_HEALTH_LIFECYCLE__:{state},__SANA_DATAROOM_NUTRITION_HISTORY__:{state},__SANA_DATAROOM_FORECAST_HISTORY__:{state},__SANA_DATAROOM_HARVEST_HISTORY__:{state},__SANA_DATAROOM_INVENTORY_HISTORY__:{state},__SANA_DATAROOM_ECONOMIC_RECONCILIATION_HISTORY__:{state},__SANA_DATAROOM_COMMERCIAL_HISTORY__:{state},__SANA_DATAROOM_DATA_TRUST_HISTORY__:{state},__SANA_DATAROOM_CAPTURE_SYNC_HISTORY__:{state},__SANA_DATAROOM_SOURCE_EVIDENCE_HISTORY__:{state},__SANA_DATAROOM_CIRCULARITY_HISTORY__:{state},__SANA_DATAROOM_MATERIAL_HISTORY__:{state},__SANA_DATAROOM_IMPACT_HISTORY__:{state},__SANA_DATAROOM_CAPITAL_GOVERNANCE_HISTORY__:{state},__SANA_DATAROOM_CAPITAL_REVIEW_HISTORY__:{state},__SANA_DATAROOM_FINDINGS_HISTORY__:{state},__SANA_DATAROOM_REVIEW_GOVERNANCE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_CASE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_HANDOFF__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_FEEDBACK__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_RESPONSE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_DISPOSITION__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_ROUND__:{cases:()=>data.refs}}}
const discovery=baseHost(),v174=context.window.__SANA_DATAROOM_EXECUTIVE_V174_FACTORY__.create(discovery).build({lot:'LOT-A'}),target174=v174.envelopes.find(e=>e.sourceId==='CAPITAL_REVIEW'&&e.claimClass==='SOURCE_REFERENCE_PRESENT');assert.ok(target174&&target174.locatorKeys.length);const locator=Array.from(target174.locatorKeys)[0],claimId=target174.claimId,envelopeId=target174.envelopeId;
const att=attFactory.create([{attestationRef:'ATT-01',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',attestationState:'REFERENCE_ONLY'}]);const ext=extFactory.create([{verificationRef:'EXT-01',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',resultRef:'RESULT-01',verificationState:'RESULT_REFERENCE_ONLY'}]);
const reviews=reviewFactory.create([{reviewRef:'OPEN-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'},{reviewRef:'OPEN-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'},{reviewRef:'OPEN-3',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'},{reviewRef:'FOLLOW-3',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewState:'SUFFICIENT_FOR_DECLARED_REVIEW_SCOPE_REFERENCE_ONLY'}]);
const handoffs=handoffFactory.create([{eventRef:'REQ-E1',requestRef:'REQ-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewRef:'OPEN-1',requestedEvidenceRefs:['D1'],kind:'EVIDENCE_REQUEST_REFERENCE_ONLY'},{eventRef:'REQ-E2',requestRef:'REQ-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewRef:'OPEN-2',requestedEvidenceRefs:['D2'],kind:'EVIDENCE_REQUEST_REFERENCE_ONLY'},{eventRef:'RESP-E2',requestRef:'REQ-2',responseRef:'RESP-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',responseEvidenceRefs:['D2-V2'],kind:'EVIDENCE_RESPONSE_REFERENCE_ONLY'},{eventRef:'REQ-E3',requestRef:'REQ-3',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewRef:'OPEN-3',requestedEvidenceRefs:['D3'],kind:'EVIDENCE_REQUEST_REFERENCE_ONLY'},{eventRef:'RESP-E3',requestRef:'REQ-3',responseRef:'RESP-3',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',responseEvidenceRefs:['D3-V2'],kind:'EVIDENCE_RESPONSE_REFERENCE_ONLY'}]);
const rounds=roundFactory.create([{roundRef:'ROUND-REQUEST',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'OPEN-1',requestRef:'REQ-1'},{roundRef:'ROUND-RESPONSE',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'OPEN-2',requestRef:'REQ-2',responseRefs:['RESP-2']},{roundRef:'ROUND-FOLLOWUP',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'OPEN-3',requestRef:'REQ-3',responseRefs:['RESP-3'],followupReviewRef:'FOLLOW-3'}]);assert.equal(rounds.records().length,3);
function hostWith(pointer=undefined,roundSource=rounds){const h=baseHost();h.__SANA_DATAROOM_CLAIM_ATTESTATION__=att;h.__SANA_DATAROOM_CLAIM_EXTERNAL_VERIFICATION__=ext;h.__SANA_DATAROOM_CLAIM_EVIDENCE_SUFFICIENCY__=reviews;h.__SANA_DATAROOM_CLAIM_EVIDENCE_HANDOFF__=handoffs;h.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND__=roundSource;if(pointer!==undefined)h.__SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND__=pointer;return h}
const parent179=context.window.__SANA_DATAROOM_EXECUTIVE_V179_FACTORY__.create(hostWith()).build({lot:'LOT-A'}),parentTarget=parent179.envelopes.find(e=>e.claimId===claimId);assert.equal(parentTarget.evidenceRemediationHistory.state,'REMEDIATION_ROUND_REFERENCES_ONLY');assert.equal(parentTarget.evidenceRemediationHistory.rounds.length,3);assert.equal(parentTarget.evidenceRemediationHistory.currentRoundDetermined,false);
const baseline=execFactory.create(hostWith(context.window.__SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND__)).build({lot:'LOT-A'}),baselineTarget=baseline.envelopes.find(e=>e.claimId===claimId);assert.equal(baselineTarget.currentRemediationRoundPointer.state,'NO_EXPLICIT_CURRENT_ROUND_POINTER');

const maliciousInput=[{pointerRef:'PTR-1',claimId,claimEnvelopeRef:envelopeId,currentRoundRef:'ROUND-FOLLOWUP',pointerState:'CURRENT_ROUND_REFERENCE_ONLY',locatorKeys:[locator],lotId:'LOT-A',observedAt:'1900-01-01',latest:true,authoritative:true,approved:true,resolved:true,score:100}];const before=JSON.stringify(maliciousInput),pointer=pointerFactory.create(maliciousInput);assert.equal(JSON.stringify(maliciousInput),before);assert.equal(pointer.records().length,1);const normalized=pointer.records()[0];assert.equal(normalized.currentRoundAuthority,false);assert.equal(normalized.latestRoundReferenceDetermined,false);assert.equal(normalized.latestRoundAuthority,false);assert.equal(normalized.chronologyDetermined,false);assert.equal(normalized.claimTruthVerified,false);assert.ok(!Object.prototype.hasOwnProperty.call(normalized,'score'));
const duplicate=pointerFactory.create([{...maliciousInput[0],pointerRef:'DUP'},{...maliciousInput[0],pointerRef:'DUP'}]);assert.equal(duplicate.records().length,0);assert.ok(duplicate.diagnostics().some(x=>x.reason==='DUPLICATE_POINTER_REF'));
const multiple=pointerFactory.create([{...maliciousInput[0],pointerRef:'P1',currentRoundRef:'ROUND-REQUEST'},{...maliciousInput[0],pointerRef:'P2',currentRoundRef:'ROUND-FOLLOWUP'}]);assert.equal(multiple.records().length,0);assert.ok(multiple.diagnostics().some(x=>x.reason==='MULTIPLE_CURRENT_POINTERS_FOR_CLAIM'));
const badState=pointerFactory.create([{...maliciousInput[0],pointerRef:'BAD-STATE',pointerState:'LATEST'}]);assert.equal(badState.records().length,0);assert.equal(badState.diagnostics()[0].reason,'CURRENT_ROUND_POINTER_STATE_NOT_ALLOWED');
const badEnvSource=pointerFactory.create([{...maliciousInput[0],pointerRef:'BAD-ENV-SOURCE',claimEnvelopeRef:'ENV::WRONG'}]);assert.equal(badEnvSource.records().length,0);assert.equal(badEnvSource.diagnostics()[0].reason,'CLAIM_ENVELOPE_REF_MISMATCH');

function build(pointerSource,roundSource=rounds){return execFactory.create(hostWith(pointerSource,roundSource)).build({lot:'LOT-A'})}
for(const [roundRef,stage] of [['ROUND-REQUEST','REQUEST_ROUND_REFERENCE_ONLY'],['ROUND-RESPONSE','REQUEST_RESPONSE_ROUND_REFERENCE_ONLY'],['ROUND-FOLLOWUP','REQUEST_RESPONSE_FOLLOWUP_REVIEW_ROUND_REFERENCE_ONLY']]){const src=pointerFactory.create([{pointerRef:`PTR-${roundRef}`,claimId,claimEnvelopeRef:envelopeId,currentRoundRef:roundRef,pointerState:'CURRENT_ROUND_REFERENCE_ONLY',locatorKeys:[locator],lotId:'LOT-A',observedAt:roundRef==='ROUND-REQUEST'?'2099-01-01':'1900-01-01'}]);const result=build(src),target=result.envelopes.find(e=>e.claimId===claimId),p=target.currentRemediationRoundPointer;assert.equal(p.state,'CURRENT_ROUND_REFERENCE_ONLY');assert.equal(p.currentRoundRef,roundRef);assert.equal(p.referencedRoundStage,stage);assert.equal(p.currentRoundReferenceDetermined,true);assert.equal(p.currentRoundAuthority,false);assert.equal(p.latestRoundRef,null);assert.equal(p.latestRoundReferenceDetermined,false);assert.equal(p.latestRoundAuthority,false);assert.equal(p.chronologyDetermined,false);assert.equal(p.chronologyAuthority,false);assert.equal(p.claimTruthVerified,false);assert.equal(p.evidenceAccepted,false);assert.equal(p.evidentiarySufficiencyDetermined,false);assert.equal(p.findingResolved,false);assert.equal(target.evidenceRemediationHistory.currentRoundRef,null,'V179 history must remain untouched');assert.equal(target.evidenceRemediationHistory.currentRoundDetermined,false);assert.equal(result.authority.currentRoundAuthority,false);assert.equal(result.authority.latestRoundAuthority,false);assert.equal(result.authority.chronologyAuthority,false);assert.equal(result.summary.latestRoundReferenceDetermined,0);assert.equal(result.summary.chronologyDetermined,0);}
const exact=build(pointer),exactTarget=exact.envelopes.find(e=>e.claimId===claimId);assert.equal(exactTarget.attestation.state,'ATTESTATION_REFERENCE_ONLY');assert.equal(exactTarget.externalVerification.state,'EXTERNAL_VERIFICATION_REFERENCE_ONLY');assert.equal(exactTarget.evidenceHandoff.state,'EVIDENCE_REQUEST_AND_RESPONSE_REFERENCE_ONLY');assert.equal(exactTarget.evidenceRemediationHistory.state,'REMEDIATION_ROUND_REFERENCES_ONLY');assert.equal(exactTarget.currentRemediationRoundPointer.referencedFollowupReviewState,'SUFFICIENT_FOR_DECLARED_REVIEW_SCOPE_REFERENCE_ONLY');assert.equal(exactTarget.currentRemediationRoundPointer.evidentiarySufficiencyDetermined,false);assert.equal(exact.summary.currentRoundReferenceDetermined,1);assert.equal(exact.summary.currentRoundAuthority,0);assert.equal(exact.summary.latestRoundReferenceDetermined,0);assert.equal(exact.summary.latestRoundAuthority,0);assert.equal(exact.audit.currentRoundReferenceLinking,true);assert.equal(exact.audit.currentRoundAuthority,false);assert.equal(exact.audit.latestRoundInference,false);assert.equal(exact.audit.chronologyInference,false);assert.equal(exact.capabilities.currentRoundReferenceDetermination,true);assert.equal(exact.capabilities.latestRoundReferenceDetermination,false);
const repeat=build(pointer);assert.equal(JSON.stringify(repeat),JSON.stringify(exact));assert.ok(Object.isFrozen(exact));assert.ok(Object.isFrozen(exactTarget.currentRemediationRoundPointer));assert.equal(JSON.stringify(exact.audit.provenanceGap),JSON.stringify(parent179.audit.provenanceGap));

function direct(records){return {schema:'SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_V1',records:()=>records,forClaim:()=>[{...records[0],current:true,latest:true}]}}
const twoDirect=build(direct([{...pointer.records()[0],pointerRef:'P-A',currentRoundRef:'ROUND-REQUEST'},{...pointer.records()[0],pointerRef:'P-B',currentRoundRef:'ROUND-FOLLOWUP'}])).envelopes.find(e=>e.claimId===claimId);assert.equal(twoDirect.currentRemediationRoundPointer.state,'CURRENT_ROUND_POINTER_CONFLICT');assert.ok(twoDirect.currentRoundPointerDiagnostic.conflictReasons.includes('MULTIPLE_CURRENT_POINTERS_FOR_CLAIM'));
const unknown=build(direct([{...pointer.records()[0],pointerRef:'P-UNKNOWN',currentRoundRef:'ROUND-NOT-FOUND'}])).envelopes.find(e=>e.claimId===claimId);assert.equal(unknown.currentRemediationRoundPointer.state,'CURRENT_ROUND_POINTER_CONFLICT');assert.ok(unknown.currentRoundPointerDiagnostic.conflictReasons.includes('CURRENT_ROUND_REF_NOT_FOUND_IN_PARENT'));
const wrongEnvelope=build(direct([{...pointer.records()[0],pointerRef:'P-BAD-ENV',claimEnvelopeRef:'ENV::WRONG'}])).envelopes.find(e=>e.claimId===claimId);assert.ok(wrongEnvelope.currentRoundPointerDiagnostic.conflictReasons.includes('CLAIM_ENVELOPE_REF_MISMATCH'));
const wrongLot=build(direct([{...pointer.records()[0],pointerRef:'P-BAD-LOT',lotId:'LOT-B'}])).envelopes.find(e=>e.claimId===claimId);assert.ok(wrongLot.currentRoundPointerDiagnostic.conflictReasons.includes('POINTER_LOT_SCOPE_CONTRADICTION'));
const wrongLocator=build(direct([{...pointer.records()[0],pointerRef:'P-BAD-LOC',locatorKeys:['LOC::OUTSIDE']}])).envelopes.find(e=>e.claimId===claimId);assert.ok(wrongLocator.currentRoundPointerDiagnostic.conflictReasons.includes('LOCATOR_OUTSIDE_CLAIM'));
const wrongClaim=build(direct([{...pointer.records()[0],pointerRef:'P-OTHER',claimId:'CLM::OTHER',claimEnvelopeRef:'ENV::CLM::OTHER'}])).envelopes.find(e=>e.claimId===claimId);assert.equal(wrongClaim.currentRemediationRoundPointer.state,'NO_EXPLICIT_CURRENT_ROUND_POINTER');
const wrongSchema=build({schema:'WRONG',records:()=>pointer.records()});assert.equal(wrongSchema.sourceContract.state,'SCHEMA_MISMATCH');const invalidApi=build({schema:'SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_V1',records:()=>({})});assert.equal(invalidApi.sourceContract.state,'INVALID_API');const maliciousHelper=build({schema:'SANA_DATAROOM_CLAIM_CURRENT_REMEDIATION_ROUND_POINTER_V1',records:()=>[],forClaim:()=>pointer.records()}).envelopes.find(e=>e.claimId===claimId);assert.equal(maliciousHelper.currentRemediationRoundPointer.state,'NO_EXPLICIT_CURRENT_ROUND_POINTER');
const badRoundDirect={schema:'SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1',records:()=>[{...rounds.records()[0],roundRef:'BROKEN-ROUND',openingReviewRef:'UNKNOWN-REVIEW'}]};const parentConflict=build(pointerFactory.create([{pointerRef:'PTR-BROKEN',claimId,claimEnvelopeRef:envelopeId,currentRoundRef:'BROKEN-ROUND',pointerState:'CURRENT_ROUND_REFERENCE_ONLY',locatorKeys:[locator],lotId:'LOT-A'}]),badRoundDirect).envelopes.find(e=>e.claimId===claimId);assert.equal(parentConflict.evidenceRemediationHistory.state,'REMEDIATION_ROUND_CONFLICT');assert.equal(parentConflict.currentRemediationRoundPointer.state,'CURRENT_ROUND_POINTER_CONFLICT');assert.ok(parentConflict.currentRoundPointerDiagnostic.conflictReasons.includes('PARENT_REMEDIATION_ROUND_HISTORY_NOT_VALID'));

const serialized=JSON.stringify(exact);for(const forbidden of ['riskScore','creditScore','investmentScore','projectScore','overallScore'])assert.ok(!serialized.includes(forbidden),`forbidden score in output: ${forbidden}`);
console.log('V180 current remediation round pointer validation PASS');