import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
const path='apps/control-web/public/sana-v3-dataroom-review-governance.js',code=fs.readFileSync(path,'utf8'),storage={records:[]};const ctx={window:{__SANA_ACCESS__:{role:'admin'}},storage,DEMO:{lots:[{id:'CAF-A1',crop:'Café'},{id:'CAC-B1',crop:'Cacao'}]},views:{dataroom:()=>'<footer class="footer"></footer>'},metric:()=>'',esc:v=>String(v),openModal:()=>{},document:{addEventListener:()=>{}},console};ctx.window.window=ctx.window;vm.createContext(ctx);vm.runInContext(code,ctx,{filename:path});const api=ctx.window.__SANA_DATAROOM_REVIEW_GOVERNANCE__;assert(api);assert.equal(api.schema,'SANA_DATAROOM_REVIEW_GOVERNANCE_V1');const s=api.summary();assert.equal(s.cases,2);assert.equal(s.assignments,2);assert.equal(s.conflictDeclarations,2);assert.equal(s.recusals,1);for(const f of ['verifiedReviewerIdentities','verifiedReviewerQualifications','verifiedReviewerIndependence','automaticDisqualifications','riskRatings','riskScores','dueDiligenceApprovals','eligibilityDecisions','investmentSignals','investmentOffers','fundingExecuted'])assert.equal(s[f],0,`${f} must remain 0`);const caf=api.cases().find(c=>c.id==='RG-CAF-01'),cac=api.cases().find(c=>c.id==='RG-CAC-01');assert(caf&&cac);assert.equal(caf.conflicts[0].conflictState,'NO_CONFLICT_DECLARED_REFERENCE_ONLY');assert.equal(caf.verifiedReviewerIndependence,0);assert.equal(cac.conflicts[0].conflictState,'POTENTIAL_CONFLICT_DECLARED_REFERENCE_ONLY');assert.equal(cac.recusals.length,1);assert.equal(cac.automaticDisqualifications,0);assert.equal(cac.riskRatings,0);assert.deepEqual([...caf.semantics.scopeWithoutAssignment],[]);assert.deepEqual([...caf.semantics.conflictWithoutAssignment],[]);assert.deepEqual([...caf.semantics.unresolvedEvidence],[]);
storage.records.push({id:'BAD-S',type:'dataroom-review-governance',lot:'BAD',createdAt:'2026-08-18T10:00:00-05:00',values:{caseId:'RG-BAD',kind:'REVIEW_SCOPE_DECLARED',assignmentRef:'A-MISSING',scopeRef:'S-BAD'}},{id:'BAD-C',type:'dataroom-review-governance',lot:'BAD',createdAt:'2026-08-18T10:01:00-05:00',values:{caseId:'RG-BAD',kind:'CONFLICT_DECLARATION_REFERENCE',assignmentRef:'A-MISSING',conflictDeclarationRef:'C-BAD'}},{id:'BAD-R',type:'dataroom-review-governance',lot:'BAD',createdAt:'2026-08-18T10:02:00-05:00',values:{caseId:'RG-BAD',kind:'RECUSAL_REFERENCE',assignmentRef:'A-MISSING',recusalRef:'R-BAD'}},{id:'BAD-X',type:'dataroom-review-governance',lot:'BAD',createdAt:'2026-08-18T10:03:00-05:00',values:{caseId:'RG-BAD',kind:'GOVERNANCE_STATUS_CHANGED',assignmentRef:'A-MISSING',statusRef:'X-BAD'}},{id:'BAD-E',type:'dataroom-review-governance',lot:'BAD',createdAt:'2026-08-18T10:04:00-05:00',values:{caseId:'RG-BAD',kind:'GOVERNANCE_EVIDENCE_REFERENCE',evidenceRef:'E-BAD',supports:'NO-EVENT'}});const bad=api.cases().find(c=>c.id==='RG-BAD');assert(bad);assert.deepEqual([...bad.semantics.scopeWithoutAssignment],['BAD-S']);assert.deepEqual([...bad.semantics.conflictWithoutAssignment],['BAD-C']);assert.deepEqual([...bad.semantics.recusalWithoutAssignment],['BAD-R']);assert.deepEqual([...bad.semantics.statusWithoutAssignment],['BAD-X']);assert.deepEqual([...bad.semantics.unresolvedEvidence],['NO-EVENT']);for(const re of [/REVIEWER_ASSIGNMENT_REFERENCE ≠ VERIFIED_IDENTITY ≠ VERIFIED_QUALIFICATION/,/REVIEW_SCOPE_DECLARED ≠ AUTHORITY_GRANTED/,/CONFLICT_DECLARATION_REFERENCE ≠ CONFLICT_FACT/,/NO_CONFLICT_DECLARED ≠ INDEPENDENCE_VERIFIED/,/CONFLICT_DECLARED ≠ DISQUALIFICATION ≠ INVESTMENT_RISK/,/RECUSAL_REFERENCE ≠ FINDING_INVALIDITY/,/GOVERNANCE_CLOSED ≠ DUE_DILIGENCE_APPROVAL/,/GOVERNANCE ≠ ELIGIBILITY ≠ INVESTMENT_DECISION ≠ INVESTMENT_OFFER/,/NO_AUTOMATIC_DISQUALIFICATION/])assert.match(api.integrity,re);assert(!/productionExecutionAvailable\s*=\s*true|productionActivationAllowed\s*=\s*true|canonicalMutated\s*=\s*true/.test(code));assert(!/fetch\s*\(/.test(code));console.log('SANA Data Room reviewer governance v73 validation: OK');
