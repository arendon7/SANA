import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
const path='apps/control-web/public/sana-v3-dataroom-executive-v183.js',code=fs.readFileSync(path,'utf8');
for(const [p,l] of [[/VERSION='V183'/,'version'],[/SANA_DATAROOM_EXECUTIVE_EXACT_GOVERNANCE_REFERENCE_RESOLUTION_V1/,'schema'],[/PARENT_SHA='2ed34661935e55fb6cd689520b93f0ca261358a7'/,'parent'],[/GOVERNANCE_REFERENCE_IDENTIFIER_RESOLUTION_EXACT_V1/,'rule'],[/EXACT_CASE_DISPOSITION_REFS_MEMBER/,'disposition exact member'],[/IDENTIFIER_RESOLVED ≠ REFERENCE_VERIFIED ≠ CASE_VALID/,'resolution boundary'],[/transitionAuthorizationAuthority:false/,'authorization false'],[/referenceVerificationAuthority:false/,'verification authority false'],[/COUNTS_ONLY ≠ SCORE/,'score boundary']])assert.ok(p.test(code),`missing V183 invariant ${l}`);
for(const bad of ['fetch(','XMLHttpRequest','localStorage.setItem','sessionStorage.setItem','indexedDB.open','canonicalMutationAvailable:true','financialMutationAvailable:true','referenceVerificationAuthority:true','governanceAuthority:true','transitionAuthorizationAuthority:true','transitionApprovalAuthority:true','complianceAuthority:true','legalEffectAuthority:true','supersessionAuthority:true','chronologyAuthority:true','dueDiligenceApprovalAuthority:true','eligibilityAuthority:true','financingApprovalAuthority:true','decisionAuthority:true','resolutionRatio:','resolutionPercentage:','riskScore:','creditScore:','investmentScore:','projectScore:','overallScore:'])assert.ok(!code.includes(bad),`forbidden V183 token ${bad}`);
const c={window:{},structuredClone,console};c.globalThis=c;vm.runInNewContext(code,c,{filename:path});const execF=c.window.__SANA_DATAROOM_EXECUTIVE_V183_FACTORY__;assert.equal(execF.version,'V183');
const claimId='CLM::A',envelopeId=`ENV::${claimId}`;
function baseEnvelope(ref1={governanceRef:'G1',governanceCaseRef:'GC-1',dispositionRef:'D-1',reviewRoundRef:'RND-1'},ref2={governanceRef:'G2',governanceCaseRef:'GC-2',dispositionRef:null,reviewRoundRef:null}){return {claimId,envelopeId,selectedLot:'LOT-A',sourceId:'CAPITAL_REVIEW',sectionId:'CAPITAL_READINESS',pointerTransitionGovernance:{state:'POINTER_TRANSITION_GOVERNANCE_REFERENCES_COMPLETE',transitionCount:2,referencedTransitionCount:2,transitions:[{predecessorPointerRef:'P1',successorPointerRef:'P2',governanceReference:ref1},{predecessorPointerRef:'P2',successorPointerRef:'P3',governanceReference:ref2}],transitionAuthorized:false,transitionApproved:false,complianceDetermined:false,legalEffectDetermined:false},claimTruthVerified:false,evidentiarySufficiencyDetermined:false,decisionAuthority:false}}
const parentF={create:h=>({build:()=>({schema:'SANA_DATAROOM_EXECUTIVE_EXACT_POINTER_TRANSITION_GOVERNANCE_COVERAGE_V1',version:'V182',scope:{lot:'LOT-A'},envelopes:[h.__PARENT__],audit:{provenanceGap:{status:'NOT_MATERIALIZED_IN_VERIFIED_GIT_LINEAGE',policy:'DO_NOT_RECONSTRUCT_MISSING_HISTORY'}}})})};c.__SANA_DATAROOM_EXECUTIVE_V182_FACTORY__=parentF;c.window.__SANA_DATAROOM_EXECUTIVE_V182_FACTORY__=parentF;
const gov=cases=>({schema:'SANA_DATAROOM_REVIEW_GOVERNANCE_V1',cases:()=>cases}),disp=cases=>({schema:'SANA_DATAROOM_REVIEW_DISPOSITION_V1',cases:()=>cases}),round=cases=>({schema:'SANA_DATAROOM_REVIEW_ROUND_V1',cases:()=>cases});
function host(parent=baseEnvelope(),g=gov([{id:'GC-1',lot:'LOT-A'},{id:'GC-2',lot:'LOT-A'}]),d=disp([{id:'DC-1',lot:'LOT-A',dispositionRefs:['D-1']}]),r=round([{id:'RND-1',lot:'LOT-A'}])){return {__PARENT__:parent,__SANA_DATAROOM_REVIEW_GOVERNANCE__:g,__SANA_DATAROOM_REVIEW_DISPOSITION__:d,__SANA_DATAROOM_REVIEW_ROUND__:r}}
function build(...args){return execF.create(host(...args)).build({lot:'LOT-A'})}function target(r){return r.envelopes[0]}
const exact=build(),t=target(exact);assert.equal(t.governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIERS_RESOLVED_EXACTLY');assert.equal(t.governanceReferenceResolution.resolvedExactlyCount,2);const z1=t.governanceReferenceResolution.transitions[0].governanceIdentifierResolution,z2=t.governanceReferenceResolution.transitions[1].governanceIdentifierResolution;assert.equal(z1.state,'IDENTIFIERS_RESOLVED_EXACTLY');assert.equal(z1.governanceCase.state,'RESOLVED_EXACTLY');assert.equal(z1.disposition.state,'RESOLVED_EXACTLY');assert.equal(z1.reviewRound.state,'RESOLVED_EXACTLY');assert.equal(z2.disposition.state,'NOT_DECLARED');assert.equal(z2.reviewRound.state,'NOT_DECLARED');assert.equal(z1.governanceCaseReferenceVerified,false);assert.equal(z1.dispositionReferenceVerified,false);assert.equal(z1.reviewRoundReferenceVerified,false);assert.equal(z1.transitionAuthorized,false);assert.equal(z1.transitionApproved,false);assert.equal(z1.complianceDetermined,false);assert.equal(z1.legalEffectDetermined,false);assert.equal(exact.summary.verifiedReferences,0);assert.equal(exact.summary.transitionAuthorizations,0);assert.equal(exact.summary.complianceDeterminations,0);assert.equal(exact.authority.referenceVerificationAuthority,false);assert.ok(Object.isFrozen(exact));assert.equal(t.pointerTransitionGovernance.state,'POINTER_TRANSITION_GOVERNANCE_REFERENCES_COMPLETE','V182 coverage state preserved');
const partialParent=baseEnvelope({governanceRef:'G1',governanceCaseRef:'GC-1',dispositionRef:'D-1',reviewRoundRef:'MISSING-RND'});const partial=build(partialParent),pt=target(partial);assert.equal(pt.governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIERS_PARTIALLY_RESOLVED');assert.equal(pt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution.state,'IDENTIFIERS_PARTIALLY_RESOLVED');assert.equal(pt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution.reviewRound.state,'UNRESOLVED');
const unresolvedParent=baseEnvelope({governanceRef:'G1',governanceCaseRef:'NO-GOV',dispositionRef:null,reviewRoundRef:null},{governanceRef:'G2',governanceCaseRef:'NO-GOV-2',dispositionRef:null,reviewRoundRef:null});const unresolved=build(unresolvedParent),ut=target(unresolved);assert.equal(ut.governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIERS_UNRESOLVED');assert.equal(ut.governanceReferenceResolution.unresolvedCount,2);
const caseIdIsNotDisposition=baseEnvelope({governanceRef:'G1',governanceCaseRef:'GC-1',dispositionRef:'DC-1',reviewRoundRef:null});const noCaseShortcut=build(caseIdIsNotDisposition),nt=target(noCaseShortcut);assert.equal(nt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution.disposition.state,'UNRESOLVED','disposition case id must not resolve dispositionRef');
const duplicateGov=build(baseEnvelope(),gov([{id:'GC-1',lot:'LOT-A'},{id:'GC-1',lot:'LOT-A'},{id:'GC-2',lot:'LOT-A'}]));assert.equal(target(duplicateGov).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIER_RESOLUTION_CONFLICT');assert.equal(target(duplicateGov).governanceReferenceResolution.transitions[0].governanceIdentifierResolution.governanceCase.state,'AMBIGUOUS');
const crossLot=build(baseEnvelope(),gov([{id:'GC-1',lot:'LOT-B'},{id:'GC-2',lot:'LOT-A'}]));assert.equal(target(crossLot).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIER_RESOLUTION_CONFLICT');assert.equal(target(crossLot).governanceReferenceResolution.transitions[0].governanceIdentifierResolution.governanceCase.state,'LOT_SCOPE_CONFLICT');
const duplicateDisp=build(baseEnvelope(),undefined,disp([{id:'DCA',lot:'LOT-A',dispositionRefs:['D-1']},{id:'DCB',lot:'LOT-A',dispositionRefs:['D-1']} ]));assert.equal(target(duplicateDisp).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIER_RESOLUTION_CONFLICT');
const wrongGovSchema=build(baseEnvelope(),{schema:'WRONG',cases:()=>[]});assert.equal(wrongGovSchema.sourceContracts.governance.state,'SCHEMA_MISMATCH');assert.equal(target(wrongGovSchema).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIER_RESOLUTION_CONFLICT');
const malformedRound=build(baseEnvelope(),undefined,undefined,{schema:'SANA_DATAROOM_REVIEW_ROUND_V1',cases:()=>({})});assert.equal(malformedRound.sourceContracts.reviewRound.state,'INVALID_API');assert.equal(target(malformedRound).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIER_RESOLUTION_CONFLICT');
const missingDisposition=build(baseEnvelope(),undefined,null,undefined);assert.equal(missingDisposition.sourceContracts.disposition.state,'MISSING');assert.equal(target(missingDisposition).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIERS_PARTIALLY_RESOLVED');
const helperTrap={schema:'SANA_DATAROOM_REVIEW_GOVERNANCE_V1',cases:()=>[{id:'GC-1',lot:'LOT-A'},{id:'GC-2',lot:'LOT-A'}],forRef:()=>[{id:'MALICIOUS',verified:true,authorized:true}]};assert.equal(target(build(baseEnvelope(),helperTrap)).governanceReferenceResolution.state,'GOVERNANCE_IDENTIFIERS_RESOLVED_EXACTLY');
const maliciousParent=baseEnvelope({governanceRef:'G1',governanceCaseRef:'GC-1',dispositionRef:'D-1',reviewRoundRef:'RND-1',verified:true,authorized:true,approved:true,compliant:true,score:100});const mt=target(build(maliciousParent));assert.equal(mt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution.transitionAuthorized,false);assert.equal(mt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution.governanceCaseReferenceVerified,false);assert.ok(!Object.prototype.hasOwnProperty.call(mt.governanceReferenceResolution.transitions[0].governanceIdentifierResolution,'score'));
assert.equal(exact.audit.provenanceGap.status,'NOT_MATERIALIZED_IN_VERIFIED_GIT_LINEAGE');assert.equal(exact.audit.provenanceGap.policy,'DO_NOT_RECONSTRUCT_MISSING_HISTORY');
console.log('SANA Data Room Executive V183 exact governance identifier resolution PASS');