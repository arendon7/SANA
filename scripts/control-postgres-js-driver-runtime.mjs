import assert from 'node:assert/strict';
import path from 'node:path';
import {pathToFileURL} from 'node:url';
const target=process.argv[2];if(!target)throw new Error('COMPILED_INVESTMENT_PORTFOLIO_INDEX_REQUIRED');
const api=await import(pathToFileURL(path.resolve(target)).href);
const {createPinnedPostgresJsPoolFactory,createProductionPostgresWiringWithPinnedDriver,resolveProductionPostgresConfig,postgresJsOptionsFromProductionConfig}=api;
const CA='-----BEGIN CERTIFICATE-----\nTEST-CA-NOT-A-REAL-SECRET\n-----END CERTIFICATE-----';
const env={AGROWAY_POSTGRES_HOST:'db.prod.example.com',AGROWAY_POSTGRES_DATABASE:'agroway',AGROWAY_POSTGRES_USER:'agroway_control',AGROWAY_POSTGRES_PASSWORD:'this-is-an-alpha14-test-password-1234',AGROWAY_POSTGRES_CA_PEM:CA,AGROWAY_POSTGRES_POOL_MAX:'7',AGROWAY_POSTGRES_CONNECT_TIMEOUT_MS:'4100',AGROWAY_POSTGRES_STATEMENT_TIMEOUT_MS:'9100'};
let capturedOptions;let ends=0;let releases=0;const queries=[];
const loader=async()=>({default(options){capturedOptions=options;return{async reserve(){return{async unsafe(text,values=[],opts={}){queries.push({text,values,opts});let rows;if(text.startsWith('SELECT current_database()'))rows=[{database:'agroway',application_name:'agroway-control'}];else rows=[{echo:values[0]??null}];Object.defineProperty(rows,'count',{value:1});return rows},release(){releases++}}},async end(){ends++}}}});
let passed=0;const check=(name,value)=>{assert.ok(value,name);passed++;console.log(`PASS ${name}`)};
const config=resolveProductionPostgresConfig(env);const options=postgresJsOptionsFromProductionConfig(config);
check('options:host',options.host===env.AGROWAY_POSTGRES_HOST);check('options:user',options.username===env.AGROWAY_POSTGRES_USER);check('options:secret-forwarded',options.password===env.AGROWAY_POSTGRES_PASSWORD);check('options:tls',options.ssl.rejectUnauthorized===true&&options.ssl.servername===env.AGROWAY_POSTGRES_HOST&&options.ssl.ca===CA);check('options:max',options.max===7);check('options:connect-timeout-ceil',options.connect_timeout===5);check('options:statement-timeout',options.connection.statement_timeout==='9100');check('options:application-name',options.connection.application_name==='agroway-control');check('options:fetch-types-off',options.fetch_types===false);
const factory=await createPinnedPostgresJsPoolFactory(loader);check('driver:identity',factory.driver.name==='postgres.js'&&factory.driver.version==='3.4.9'&&factory.driver.dependencyCount===0);check('driver:no-secret-metadata',!JSON.stringify(factory.driver).includes(env.AGROWAY_POSTGRES_PASSWORD));
const pool=factory.create(config);check('factory:captured-options',capturedOptions.password===env.AGROWAY_POSTGRES_PASSWORD);const client=await pool.connect();const result=await client.query('SELECT $1::text AS echo',['bridge-ok']);check('bridge:parameterized',queries.at(-1).values[0]==='bridge-ok'&&queries.at(-1).opts.prepare===true);check('bridge:rows',result.rows[0].echo==='bridge-ok');check('bridge:row-count',result.rowCount===1);client.release();check('bridge:release',releases===1);await factory.close();check('bridge:close',ends===1);
const wiring=await createProductionPostgresWiringWithPinnedDriver(env,loader);check('wiring:driver',wiring.driver.version==='3.4.9');assert.throws(()=>wiring.createCanonicalUnitOfWorkAfterConnectivityCertification(),/PRODUCTION_POSTGRES_CONNECTIVITY_CERTIFICATION_REQUIRED/);check('wiring:before-probe-fails-closed',true);const evidence=await wiring.verifyConnectivity();check('wiring:probe',evidence.state==='CONNECTED_READ_ONLY_PROBE'&&evidence.canonicalWriteExecuted===false);const uow=wiring.createCanonicalUnitOfWorkAfterConnectivityCertification();check('wiring:uow-after-probe',typeof uow.transaction==='function');check('wiring:no-canonical-write',wiring.canonicalWriteExecuted===false);await wiring.closeDriver();check('wiring:closed',ends===2);
let timeoutEnds=0;const timeoutLoader=async()=>({default(){return{reserve(){return new Promise(()=>{})},async end(){timeoutEnds++}}}});const timeoutEnv={...env,AGROWAY_POSTGRES_CONNECT_TIMEOUT_MS:'500'};const timeoutConfig=resolveProductionPostgresConfig(timeoutEnv);const timeoutFactory=await createPinnedPostgresJsPoolFactory(timeoutLoader);const timeoutPool=timeoutFactory.create(timeoutConfig);const started=Date.now();await assert.rejects(()=>timeoutPool.connect(),/POSTGRES_JS_CONNECT_TIMEOUT/);const elapsed=Date.now()-started;check('timeout:outer-fail-closed',elapsed>=700&&elapsed<2500);check('timeout:driver-destroyed',timeoutEnds===1);await assert.rejects(()=>timeoutPool.connect(),/POSTGRES_JS_POOL_DESTROYED/);check('timeout:no-reuse-after-destroy',true);await timeoutFactory.close();check('timeout:close-idempotent',timeoutEnds===1);
console.log(`PASS_CONTROL_POSTGRES_JS_DRIVER_RUNTIME ${passed}/${passed}`);
