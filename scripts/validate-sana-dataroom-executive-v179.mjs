import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';

const paths={v171:'apps/control-web/public/sana-v3-dataroom-executive-v171.js',v172:'apps/control-web/public/sana-v3-dataroom-executive-v172.js',v173:'apps/control-web/public/sana-v3-dataroom-executive-v173.js',v174:'apps/control-web/public/sana-v3-dataroom-executive-v174.js',source175:'apps/control-web/public/sana-v3-dataroom-claim-attestation-v175.js',v175:'apps/control-web/public/sana-v3-dataroom-executive-v175.js',source176:'apps/control-web/public/sana-v3-dataroom-claim-external-verification-v176.js',v176:'apps/control-web/public/sana-v3-dataroom-executive-v176.js',source177:'apps/control-web/public/sana-v3-dataroom-claim-evidence-sufficiency-v177.js',v177:'apps/control-web/public/sana-v3-dataroom-executive-v177.js',source178:'apps/control-web/public/sana-v3-dataroom-claim-evidence-handoff-v178.js',v178:'apps/control-web/public/sana-v3-dataroom-executive-v178.js',source179:'apps/control-web/public/sana-v3-dataroom-claim-evidence-remediation-round-v179.js',v179:'apps/control-web/public/sana-v3-dataroom-executive-v179.js'};
const code=Object.fromEntries(Object.entries(paths).map(([k,p])=>[k,fs.readFileSync(p,'utf8')]));
for(const [pattern,label] of [[/VERSION='V179'/,'version'],[/SANA_DATAROOM_EXECUTIVE_EXACT_REMEDIATION_ROUND_HISTORY_V1/,'schema'],[/PARENT_SHA='b65c1219d97492e31570e34a8cb086b3465ac796'/,'parent sha'],[/CLAIM_EVIDENCE_REMEDIATION_ROUND_EXACT_V1/,'typed rule'],[/NO_TIMESTAMP_SOURCE_ORDER_OR_REF_ORDER_INFERENCE/,'no chronology inference'],[/NOT_DETERMINED_WITHOUT_EXPLICIT_POINTER_CONTRACT/,'no current pointer inference'],[/currentRoundAuthority:false/,'current authority false'],[/latestRoundAuthority:false/,'latest authority false'],[/chronologyAuthority:false/,'chronology authority false'],[/COUNTS_ONLY ≠ SCORE/,'score boundary']])assert.ok(pattern.test(code.v179),`missing V179 executive invariant: ${label}`);
for(const [pattern,label] of [[/SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1/,'source schema'],[/OPENING_REVIEW_REF_REQUIRED/,'opening review required'],[/REQUEST_REF_REQUIRED/,'request required'],[/FOLLOWUP_REVIEW_REQUIRES_RESPONSE_REF/,'followup requires response'],[/FOLLOWUP_REVIEW_MUST_DIFFER_FROM_OPENING/,'followup differs'],[/DUPLICATE_ROUND_REF/,'duplicate round rejection'],[/DUPLICATE_ROUND_BINDING/,'duplicate binding rejection'],[/NO_CURRENT_OR_LATEST_INFERENCE/,'no current latest source inference']])assert.ok(pattern.test(code.source179),`missing V179 source invariant: ${label}`);
for(const src of [code.source179,code.v179])for(const forbidden of ['fetch(','XMLHttpRequest','localStorage.setItem','sessionStorage.setItem','indexedDB.open','canonicalMutationAvailable:true','financialMutationAvailable:true','currentRoundAuthority:true','latestRoundAuthority:true','chronologyAuthority:true','claimTruthAuthority:true','evidenceAcceptanceAuthority:true','evidentiarySufficiencyAuthority:true','findingResolutionAuthority:true','dueDiligenceApprovalAuthority:true','eligibilityAuthority:true','financingApprovalAuthority:true','decisionAuthority:true','riskScore:','creditScore:','investmentScore:','projectScore:','overallScore:'])assert.ok(!src.includes(forbidden),`forbidden V179 token: ${forbidden}`);

const context={window:{},structuredClone,console};context.globalThis=context;for(const key of Object.keys(paths))vm.runInNewContext(code[key],context,{filename:paths[key]});
const attFactory=context.window.__SANA_DATAROOM_CLAIM_ATTESTATION_V175_FACTORY__,extFactory=context.window.__SANA_DATAROOM_CLAIM_EXTERNAL_VERIFICATION_V176_FACTORY__,reviewFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_SUFFICIENCY_V177_FACTORY__,handoffFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_HANDOFF_V178_FACTORY__,roundFactory=context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V179_FACTORY__,execFactory=context.window.__SANA_DATAROOM_EXECUTIVE_V179_FACTORY__;
assert.equal(roundFactory.schema,'SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1');assert.equal(context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND__.records().length,0);assert.equal(execFactory.schema,'SANA_DATAROOM_EXECUTIVE_EXACT_REMEDIATION_ROUND_HISTORY_V1');assert.equal(execFactory.version,'V179');

const data={snapshots:[{id:'SNAP-A',reportType:'RPT-DD',cutoff:'2026-08-20',manifest:{schema:'SANA_DUE_DILIGENCE_SNAPSHOT_V1'}}],state:{valid:true,state:'CAPTURED',snapshot:{id:'SNAP-A',cutoff:'2026-08-20'},rows:[{id:'ROW-A',lotId:'LOT-A'},{id:'ROW-B',lotId:'LOT-B'}],integrity:'SNAPSHOT_ONLY'},capital:[{id:'CAP-A',lot:'LOT-A',events:[{id:'CAP-A-E1',lot:'LOT-A',kind:'REVIEW_STARTED'}]},{id:'CAP-B',lot:'LOT-B',events:[{id:'CAP-B-E1',lot:'LOT-B',kind:'REVIEW_STARTED'}]}],refs:[{id:'RG-A',lot:'LOT-A',events:[{id:'RG-A-E1',lot:'LOT-A'}]},{id:'RG-B',lot:'LOT-B',events:[{id:'RG-B-E1',lot:'LOT-B'}]}]};
const state=()=>data.state;
function baseHost(){return {DEMO:{lots:[{id:'LOT-A'},{id:'LOT-B'}]},__SANA_DATAROOM_360__:{state},__SANA_DUE_DILIGENCE_SNAPSHOT__:{snapshots:()=>data.snapshots},__SANA_CAPITAL_REVIEW__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_CAPITAL_GOVERNANCE__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_DATAROOM_FINDINGS__:{cases:()=>data.capital,forLot:lot=>data.capital.filter(x=>x.lot===lot)},__SANA_DUE_DILIGENCE_GAPS__:{current:()=>({valid:true,state:'CAPTURED',snapshot:data.state.snapshot,gaps:[{id:'GAP-A',lot:'LOT-A'},{id:'GAP-B',lot:'LOT-B'}]})},__SANA_DATAROOM_PHENOLOGY_HISTORY__:{state},__SANA_DATAROOM_LABOR_HISTORY__:{state},__SANA_DATAROOM_HEALTH_HISTORY__:{state},__SANA_DATAROOM_HEALTH_LIFECYCLE__:{state},__SANA_DATAROOM_NUTRITION_HISTORY__:{state},__SANA_DATAROOM_FORECAST_HISTORY__:{state},__SANA_DATAROOM_HARVEST_HISTORY__:{state},__SANA_DATAROOM_INVENTORY_HISTORY__:{state},__SANA_DATAROOM_ECONOMIC_RECONCILIATION_HISTORY__:{state},__SANA_DATAROOM_COMMERCIAL_HISTORY__:{state},__SANA_DATAROOM_DATA_TRUST_HISTORY__:{state},__SANA_DATAROOM_CAPTURE_SYNC_HISTORY__:{state},__SANA_DATAROOM_SOURCE_EVIDENCE_HISTORY__:{state},__SANA_DATAROOM_CIRCULARITY_HISTORY__:{state},__SANA_DATAROOM_MATERIAL_HISTORY__:{state},__SANA_DATAROOM_IMPACT_HISTORY__:{state},__SANA_DATAROOM_CAPITAL_GOVERNANCE_HISTORY__:{state},__SANA_DATAROOM_CAPITAL_REVIEW_HISTORY__:{state},__SANA_DATAROOM_FINDINGS_HISTORY__:{state},__SANA_DATAROOM_REVIEW_GOVERNANCE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_CASE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_HANDOFF__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_FEEDBACK__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_RESPONSE__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_DISPOSITION__:{cases:()=>data.refs},__SANA_DATAROOM_REVIEW_ROUND__:{cases:()=>data.refs}}}
const discovery=baseHost(),v174=context.window.__SANA_DATAROOM_EXECUTIVE_V174_FACTORY__.create(discovery).build({lot:'LOT-A'}),target174=v174.envelopes.find(e=>e.sourceId==='CAPITAL_REVIEW'&&e.claimClass==='SOURCE_REFERENCE_PRESENT');assert.ok(target174&&target174.locatorKeys.length);const locator=Array.from(target174.locatorKeys)[0];
const claimId=target174.claimId,envelopeId=target174.envelopeId;
const att=attFactory.create([{attestationRef:'ATT-01',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewerRef:'REV-ATT',attestationState:'REFERENCE_ONLY'}]);
const ext=extFactory.create([{verificationRef:'EXT-01',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',providerRef:'PROVIDER-01',resultRef:'RESULT-01',verificationState:'RESULT_REFERENCE_ONLY'}]);
const reviewRecords=[
 {reviewRef:'REV-OPEN-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewerRef:'REVIEWER-1',reviewScopeRef:'SCOPE-1',requestedEvidenceRefs:['DOC-A'],observedAt:'2026-08-10',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'},
 {reviewRef:'REV-FOLLOW-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewerRef:'REVIEWER-2',reviewScopeRef:'SCOPE-1',observedAt:'2026-08-09',reviewState:'SUFFICIENT_FOR_DECLARED_REVIEW_SCOPE_REFERENCE_ONLY'},
 {reviewRef:'REV-OPEN-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewerRef:'REVIEWER-3',reviewScopeRef:'SCOPE-2',requestedEvidenceRefs:['DOC-B'],observedAt:'2026-08-30',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'},
 {reviewRef:'REV-FOLLOW-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewerRef:'REVIEWER-4',reviewScopeRef:'SCOPE-2',observedAt:'2026-08-01',reviewState:'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY'}
];
const reviews=reviewFactory.create(reviewRecords);assert.equal(reviews.records().length,4);
const handoffRecords=[
 {eventRef:'EV-REQ-1',requestRef:'REQ-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewRef:'REV-OPEN-1',requestRecipientRef:'RECIP-1',requestedEvidenceRefs:['DOC-A'],kind:'EVIDENCE_REQUEST_REFERENCE_ONLY'},
 {eventRef:'EV-RESP-1',requestRef:'REQ-1',responseRef:'RESP-1',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',responseProviderRef:'PROV-1',responseEvidenceRefs:['DOC-A-V2'],kind:'EVIDENCE_RESPONSE_REFERENCE_ONLY'},
 {eventRef:'EV-REQ-2',requestRef:'REQ-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',reviewRef:'REV-OPEN-2',requestRecipientRef:'RECIP-2',requestedEvidenceRefs:['DOC-B'],kind:'EVIDENCE_REQUEST_REFERENCE_ONLY'},
 {eventRef:'EV-RESP-2',requestRef:'REQ-2',responseRef:'RESP-2',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',responseProviderRef:'PROV-2',responseEvidenceRefs:['DOC-B-V2'],kind:'EVIDENCE_RESPONSE_REFERENCE_ONLY'}
];
const handoffs=handoffFactory.create(handoffRecords);assert.equal(handoffs.records().length,4);
function hostWith(rounds=undefined,reviewSource=reviews,handoffSource=handoffs){const h=baseHost();h.__SANA_DATAROOM_CLAIM_ATTESTATION__=att;h.__SANA_DATAROOM_CLAIM_EXTERNAL_VERIFICATION__=ext;h.__SANA_DATAROOM_CLAIM_EVIDENCE_SUFFICIENCY__=reviewSource;h.__SANA_DATAROOM_CLAIM_EVIDENCE_HANDOFF__=handoffSource;if(rounds!==undefined)h.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND__=rounds;return h}

const baseline=execFactory.create(hostWith(context.window.__SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND__)).build({lot:'LOT-A'}),baselineTarget=baseline.envelopes.find(e=>e.claimId===claimId);assert.equal(baselineTarget.evidenceRemediationHistory.state,'NO_EXPLICIT_REMEDIATION_ROUND');assert.equal(baselineTarget.evidenceRemediationHistory.currentRoundRef,null);assert.equal(baselineTarget.evidenceRemediationHistory.latestRoundRef,null);

const requestOnlyInput=[{roundRef:'ROUND-REQ',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1',responseRefs:[],observedAt:'2099-01-01',current:true,latest:true,closed:true,resolved:true,approved:true,score:100}];const requestOnlyBefore=JSON.stringify(requestOnlyInput),requestOnlySource=roundFactory.create(requestOnlyInput);assert.equal(JSON.stringify(requestOnlyInput),requestOnlyBefore);assert.equal(requestOnlySource.records().length,1);const normalized=requestOnlySource.records()[0];assert.equal(normalized.currentRoundDetermined,false);assert.equal(normalized.latestRoundDetermined,false);assert.equal(normalized.chronologyDetermined,false);assert.equal(normalized.evidenceAccepted,false);assert.equal(normalized.findingResolved,false);assert.equal(normalized.financingApproved,false);assert.ok(!Object.prototype.hasOwnProperty.call(normalized,'score'));
const requestOnly=execFactory.create(hostWith(requestOnlySource)).build({lot:'LOT-A'}),requestOnlyTarget=requestOnly.envelopes.find(e=>e.claimId===claimId);assert.equal(requestOnlyTarget.evidenceRemediationHistory.state,'REMEDIATION_ROUND_REFERENCES_ONLY');assert.equal(requestOnlyTarget.evidenceRemediationHistory.rounds[0].stage,'REQUEST_ROUND_REFERENCE_ONLY');

const responseSource=roundFactory.create([{roundRef:'ROUND-RESP',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1',responseRefs:['RESP-1']}]);const responseTarget=execFactory.create(hostWith(responseSource)).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId);assert.equal(responseTarget.evidenceRemediationHistory.rounds[0].stage,'REQUEST_RESPONSE_ROUND_REFERENCE_ONLY');assert.equal(JSON.stringify(Array.from(responseTarget.evidenceRemediationHistory.rounds[0].responseEventRefs)),JSON.stringify(['EV-RESP-1']));

const multiInput=[
 {roundRef:'ROUND-Z',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1',responseRefs:['RESP-1'],followupReviewRef:'REV-FOLLOW-1',observedAt:'2026-08-30'},
 {roundRef:'ROUND-A',claimId,claimEnvelopeRef:envelopeId,locatorKeys:[locator],lotId:'LOT-A',openingReviewRef:'REV-OPEN-2',requestRef:'REQ-2',responseRefs:['RESP-2'],followupReviewRef:'REV-FOLLOW-2',observedAt:'2026-08-01'}
];const multiBefore=JSON.stringify(multiInput),multiSource=roundFactory.create(multiInput);assert.equal(JSON.stringify(multiInput),multiBefore);assert.equal(multiSource.records().length,2);const multiHost=hostWith(multiSource),multiResult=execFactory.create(multiHost).build({lot:'LOT-A'}),multiTarget=multiResult.envelopes.find(e=>e.claimId===claimId);assert.equal(multiTarget.attestation.state,'ATTESTATION_REFERENCE_ONLY');assert.equal(multiTarget.externalVerification.state,'EXTERNAL_VERIFICATION_REFERENCE_ONLY');assert.equal(multiTarget.evidenceHandoff.state,'EVIDENCE_REQUEST_AND_RESPONSE_REFERENCE_ONLY');assert.equal(multiTarget.evidenceRemediationHistory.state,'REMEDIATION_ROUND_REFERENCES_ONLY');assert.equal(multiTarget.evidenceRemediationHistory.rounds.length,2);assert.equal(JSON.stringify(Array.from(multiTarget.evidenceRemediationHistory.rounds).map(r=>r.roundRef)),JSON.stringify(['ROUND-A','ROUND-Z']));const roundA=multiTarget.evidenceRemediationHistory.rounds.find(r=>r.roundRef==='ROUND-A'),roundZ=multiTarget.evidenceRemediationHistory.rounds.find(r=>r.roundRef==='ROUND-Z');assert.equal(roundA.stage,'REQUEST_RESPONSE_FOLLOWUP_REVIEW_ROUND_REFERENCE_ONLY');assert.equal(roundZ.stage,'REQUEST_RESPONSE_FOLLOWUP_REVIEW_ROUND_REFERENCE_ONLY');assert.equal(roundA.followupReviewState,'ADDITIONAL_EVIDENCE_REQUIRED_REFERENCE_ONLY');assert.equal(roundZ.followupReviewState,'SUFFICIENT_FOR_DECLARED_REVIEW_SCOPE_REFERENCE_ONLY');assert.equal(multiTarget.evidenceRemediationHistory.currentRoundRef,null);assert.equal(multiTarget.evidenceRemediationHistory.latestRoundRef,null);assert.equal(multiTarget.evidenceRemediationHistory.currentRoundDetermined,false);assert.equal(multiTarget.evidenceRemediationHistory.latestRoundDetermined,false);assert.equal(multiTarget.evidenceRemediationHistory.chronologyDetermined,false);assert.equal(roundA.currentRoundDetermined,false);assert.equal(roundZ.latestRoundDetermined,false);assert.equal(roundA.roundClosed,false);assert.equal(multiResult.summary.currentRoundDetermined,0);assert.equal(multiResult.summary.latestRoundDetermined,0);assert.equal(multiResult.summary.chronologyDetermined,0);assert.equal(multiResult.summary.totalRoundReferences,2);assert.equal(multiResult.authority.currentRoundAuthority,false);assert.equal(multiResult.authority.latestRoundAuthority,false);assert.equal(multiResult.authority.chronologyAuthority,false);assert.equal(multiResult.audit.currentRoundInference,false);assert.equal(multiResult.audit.latestRoundInference,false);assert.equal(multiResult.audit.chronologyInference,false);assert.equal(multiResult.capabilities.multipleRoundsPerClaim,true);assert.equal(multiResult.capabilities.currentRoundSelection,false);assert.equal(multiResult.capabilities.latestRoundInference,false);assert.equal(multiResult.capabilities.timestampOrderingInference,false);
const repeat=execFactory.create(multiHost).build({lot:'LOT-A'});assert.equal(JSON.stringify(repeat),JSON.stringify(multiResult));assert.ok(Object.isFrozen(multiResult));assert.ok(Object.isFrozen(multiTarget.evidenceRemediationHistory.rounds));
const parent178=context.window.__SANA_DATAROOM_EXECUTIVE_V178_FACTORY__.create(multiHost).build({lot:'LOT-A'});assert.equal(JSON.stringify(multiResult.audit.provenanceGap),JSON.stringify(parent178.audit.provenanceGap));

const badFollowupNoResponse=roundFactory.create([{roundRef:'BAD-FOLLOW-NO-RESP',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1',followupReviewRef:'REV-FOLLOW-1'}]);assert.equal(badFollowupNoResponse.records().length,0);assert.equal(badFollowupNoResponse.diagnostics()[0].reason,'FOLLOWUP_REVIEW_REQUIRES_RESPONSE_REF');
const sameFollowup=roundFactory.create([{roundRef:'BAD-SAME',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1',responseRefs:['RESP-1'],followupReviewRef:'REV-OPEN-1'}]);assert.equal(sameFollowup.records().length,0);assert.equal(sameFollowup.diagnostics()[0].reason,'FOLLOWUP_REVIEW_MUST_DIFFER_FROM_OPENING');
const duplicates=roundFactory.create([{roundRef:'DUP',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1'},{roundRef:'DUP',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-2',requestRef:'REQ-2'}]);assert.equal(duplicates.records().length,0);assert.ok(duplicates.diagnostics().some(x=>x.reason==='DUPLICATE_ROUND_REF'));
const duplicateBinding=roundFactory.create([{roundRef:'BIND-1',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1'},{roundRef:'BIND-2',claimId,claimEnvelopeRef:envelopeId,openingReviewRef:'REV-OPEN-1',requestRef:'REQ-1'}]);assert.equal(duplicateBinding.records().length,0);assert.ok(duplicateBinding.diagnostics().some(x=>x.reason==='DUPLICATE_ROUND_BINDING'));

function directRound(record){return {schema:'SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1',records:()=>[record],forClaim:()=>[{...record,current:true,latest:true,closed:true}]}}
function buildDirect(record,reviewSource=reviews,handoffSource=handoffs){return execFactory.create(hostWith(directRound(record),reviewSource,handoffSource)).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId)}
const sufficientOpening={...multiSource.records()[0],roundRef:'BAD-OPEN-STATE',openingReviewRef:'REV-FOLLOW-1',requestRef:'REQ-1',followupReviewRef:null,responseRefs:[]};assert.ok(buildDirect(sufficientOpening).remediationRoundDiagnostic.conflictReasons.includes('OPENING_REVIEW_MUST_REQUIRE_ADDITIONAL_EVIDENCE'));
const requestMismatch={...multiSource.records()[0],roundRef:'BAD-REQ-REVIEW',openingReviewRef:'REV-OPEN-2',requestRef:'REQ-1',responseRefs:[],followupReviewRef:null};assert.ok(buildDirect(requestMismatch).remediationRoundDiagnostic.conflictReasons.includes('REQUEST_OPENING_REVIEW_REF_MISMATCH'));
const unknownResponse={...multiSource.records()[0],roundRef:'BAD-RESP',responseRefs:['RESP-UNKNOWN'],followupReviewRef:null};assert.ok(buildDirect(unknownResponse).remediationRoundDiagnostic.conflictReasons.includes('RESPONSE_REF_NOT_FOUND'));
const crossRequestResponse={...multiSource.records()[0],roundRef:'BAD-CROSS-RESP',requestRef:'REQ-2',openingReviewRef:'REV-OPEN-2',responseRefs:['RESP-1'],followupReviewRef:null};assert.ok(buildDirect(crossRequestResponse).remediationRoundDiagnostic.conflictReasons.includes('RESPONSE_REQUEST_REF_MISMATCH'));
const unknownFollowup={...multiSource.records()[0],roundRef:'BAD-FOLLOW',followupReviewRef:'REV-UNKNOWN'};assert.ok(buildDirect(unknownFollowup).remediationRoundDiagnostic.conflictReasons.includes('FOLLOWUP_REVIEW_REF_NOT_FOUND'));
const badEnvelope={...multiSource.records()[0],roundRef:'BAD-ENV',claimEnvelopeRef:'ENV::WRONG',followupReviewRef:null,responseRefs:[]};assert.ok(buildDirect(badEnvelope).remediationRoundDiagnostic.conflictReasons.includes('CLAIM_ENVELOPE_REF_MISMATCH'));
const badLocator={...multiSource.records()[0],roundRef:'BAD-LOC',locatorKeys:['LOC::OUTSIDE'],followupReviewRef:null,responseRefs:[]};assert.ok(buildDirect(badLocator).remediationRoundDiagnostic.conflictReasons.includes('LOCATOR_OUTSIDE_CLAIM'));
const badLot={...multiSource.records()[0],roundRef:'BAD-LOT',lotId:'LOT-B',followupReviewRef:null,responseRefs:[]};assert.ok(buildDirect(badLot).remediationRoundDiagnostic.conflictReasons.includes('ROUND_LOT_SCOPE_CONTRADICTION'));
const wrongClaim={...multiSource.records()[0],roundRef:'OTHER-CLAIM',claimId:'CLM::OTHER',claimEnvelopeRef:'ENV::CLM::OTHER'};const wrongClaimResult=execFactory.create(hostWith(directRound(wrongClaim))).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId);assert.equal(wrongClaimResult.evidenceRemediationHistory.state,'NO_EXPLICIT_REMEDIATION_ROUND');
const maliciousHelper={schema:'SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1',records:()=>[],forClaim:()=>multiInput};const maliciousResult=execFactory.create(hostWith(maliciousHelper)).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId);assert.equal(maliciousResult.evidenceRemediationHistory.state,'NO_EXPLICIT_REMEDIATION_ROUND');
const wrongRoundSchema=execFactory.create(hostWith({schema:'WRONG',records:()=>multiInput})).build({lot:'LOT-A'});assert.equal(wrongRoundSchema.sourceContracts.rounds.state,'SCHEMA_MISMATCH');
const invalidRoundApi=execFactory.create(hostWith({schema:'SANA_DATAROOM_CLAIM_EVIDENCE_REMEDIATION_ROUND_V1',records:()=>({})})).build({lot:'LOT-A'});assert.equal(invalidRoundApi.sourceContracts.rounds.state,'INVALID_API');
const dependencyReviewConflict=execFactory.create(hostWith(multiSource,{schema:'WRONG',records:()=>reviewRecords},handoffs)).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId);assert.equal(dependencyReviewConflict.evidenceRemediationHistory.state,'REMEDIATION_ROUND_CONFLICT');assert.ok(dependencyReviewConflict.remediationRoundDiagnostic.conflictReasons.includes('REVIEW_SOURCE_SCHEMA_MISMATCH'));
const dependencyHandoffConflict=execFactory.create(hostWith(multiSource,reviews,{schema:'WRONG',records:()=>handoffRecords})).build({lot:'LOT-A'}).envelopes.find(e=>e.claimId===claimId);assert.equal(dependencyHandoffConflict.evidenceRemediationHistory.state,'REMEDIATION_ROUND_CONFLICT');assert.ok(dependencyHandoffConflict.remediationRoundDiagnostic.conflictReasons.includes('HANDOFF_SOURCE_SCHEMA_MISMATCH'));

for(const obj of [multiResult,multiTarget.evidenceRemediationHistory,...multiTarget.evidenceRemediationHistory.rounds]){assert.equal(obj.claimTruthVerified??false,false);assert.equal(obj.evidenceAccepted??false,false);assert.equal(obj.evidentiarySufficiencyDetermined??false,false);assert.equal(obj.findingResolved??false,false);assert.equal(obj.decisionAuthority??false,false);}
const serialized=JSON.stringify(multiResult);for(const forbidden of ['riskScore','creditScore','investmentScore','projectScore','overallScore'])assert.ok(!serialized.includes(forbidden),`forbidden score in output: ${forbidden}`);
console.log('V179 evidence remediation round history validation PASS');